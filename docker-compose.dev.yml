services:
  dev-myworkspace-frontend:
    container_name: dev-myworkspace-frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    restart: always
    ports:
      - "18080:80"
    networks:
      - dev-myworkspace-net

networks:
  dev-myworkspace-net:
    driver: bridge
