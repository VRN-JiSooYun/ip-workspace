## Patent Insight color tone alignment

- `frontend/src/pages/PatentInsight.tsx`
  - Metric card value color를 Ant Design primary token 계열로 변경했습니다.
  - Line, bar, donut, heatmap chart 색상을 독립적인 blue palette에서 frontend 기본 primary color token 계열로 맞췄습니다.
  - Chart axis, grid, tooltip 색상도 하드코딩 값 대신 Ant Design theme token을 사용하도록 정리했습니다.
  - Toolbar에 `Mock Data` 버튼을 추가해 API 호출 없이 mock statistics를 즉시 표시할 수 있게 했습니다.
  - Mock 표시 중에는 info alert를 노출하고, `검색` 버튼을 누르면 API 통계 조회 상태로 복귀하도록 상태를 분리했습니다.
  - Filing language count donut chart의 `Total` value 라벨을 pie center label로 변경해 donut 중앙에 고정되도록 수정했습니다.
  - 날짜 필터 기본 범위와 preset을 `2000-2023`에서 `1970-현재년도`로 변경했습니다.
  - Filing language count donut chart 데이터를 `Korean`, `English`, `中文`, `Japanese`, `기타` 최대 5개 그룹으로 합산해 표시하도록 정규화했습니다.
  - Filing language count donut chart 표시 순서를 count 내림차순으로 변경하되, `기타`는 항상 마지막에 표시되도록 고정했습니다.
  - 페이지 최초 로드 시 mock statistics를 표시하지 않고 현재 필터 조건으로 API 통계 조회가 실행되도록 초기 `loadStatistics()` effect를 복구했습니다.
  - API 통계 조회 실패 시 자동 mock fallback으로 전환하지 않고 API error 상태를 표시하도록 변경했습니다.
  - `검색`/`Statistics Refresh` 버튼 내부 loading 표시를 제거하고, 검색 영역 아래 결과 영역 전체가 흐려지며 중앙 spinner가 표시되도록 변경했습니다.
  - Patent Insight 검색 버튼명을 `Apply`에서 `검색`으로 변경하고, API 조회 중에는 버튼/Enter 재검색이 실행되지 않도록 막았습니다.
  - 상단 title/description block을 제거하고, 검색 영역을 MyBoard와 동일한 `c-card compact-filter-card` 기반 UX로 변경했습니다.
  - Patent Insight 검색 카드에서 날짜/Applicant 검색은 상단에 유지하고, Applicants/Targets Top N 조건은 `상세 필터 열기` 패널로 분리했습니다.
  - 상세 필터의 `Top N` prefix label을 제거하고, InputNumber 단독 입력 컨트롤로 변경했습니다.
  - `Mock Data`, `Refresh` 버튼을 상단 검색줄에서 상세 필터 패널의 Actions 영역으로 이동했습니다.
  - 상세 필터 Actions 영역의 `Refresh` 버튼명을 `Statistics Refresh`로 변경했습니다.
  - Company count 테이블이 카드 영역 밖으로 넘치지 않도록 전용 class와 내부 table body scroll 제약을 추가했습니다.
  - Target x Applicant heatmap X축 year를 날짜 검색 조건 범위로 제한하고, 종료 연도가 데이터에 없어도 마지막 X축 label로 표시되도록 수정했습니다.
  - Target x Applicant heatmap X축 label interval을 보정해 마지막 연도 label이 자동 생략되지 않도록 고정했습니다.
  - 날짜 필터 preset에 `Last 10 years`를 추가했습니다.
  - 작은 화면에서 chart 영역이 아래로 밀릴 때 화면 하단이 잘리지 않도록 Patent Insight page root에 자체 vertical scroll container를 적용했습니다.
  - Patent per Patent Type chart의 Y축 label 영역을 넓히고 긴 label은 줄바꿈되도록 조정했습니다.
  - Patent per Patent Type chart의 Y축 category label이 자동 생략되지 않도록 `interval: 0`과 overlap 표시 옵션을 적용했습니다.
  - Patent per Patent Type chart 표시 데이터를 상위 7개로 제한했습니다.
  - Patent per Patent Office chart 표시 데이터를 상위 7개로 제한했습니다.

- `frontend/src/mocks/patentInsight.ts`
  - Filing language mock label을 chart 표시 그룹과 동일하게 `中文`, `기타`로 변경했습니다.

- `frontend/src/index.css`
  - Patent Insight number control label 배경을 정의되지 않은 `--bg-secondary` 대신 전역 `--card-bg`로 변경했습니다.
  - Patent Insight 검색 카드용 responsive flex control 스타일을 추가했습니다.
  - Patent Insight 상세 필터 InputNumber의 height/line-height를 맞춰 입력 텍스트가 box 중앙에 오도록 보정했습니다.
  - Patent Insight page scroll container에 light/dark theme별 scrollbar color를 적용했습니다.
  - 작은 화면에서 Target x Applicant heatmap이 아래로 내려갈 때 chart가 작게 접히지 않도록 responsive height/min-height를 보강했습니다.
  - Patent Insight Company count table의 내부 overflow/height 스타일을 보정했습니다.
  - Patent Insight 결과 영역 loading overlay가 중앙에 표시되도록 결과 영역 최소 높이 스타일을 추가했습니다.

- 검증
  - 사용자가 빌드/실행을 수행하는 프로젝트 지침에 따라 로컬 build/test는 실행하지 않았습니다.

## Reaction Predictor label update

- `frontend/src/pages/ReactionPredictor.tsx`
  - `ΔΔG‡ Profile` 영역 타이틀을 `ΔG‡OA Profile`로 변경했습니다.
  - `ΔG‡OA Profile` 영역의 기존 molecule structure + site value pill UI를 sample profile image 표시로 교체했습니다.
  - 선택 row별 sample profile image 매핑을 추가해 추후 API image URL 필드로 대체하기 쉽게 구성했습니다.
  - status가 `calculating`인 row는 profile sample image 대신 `Calculating...` 상태 UI를 표시하도록 분기했습니다.

- `frontend/src/assets/reaction_predictor/`
  - `sample/reaction_predictor/`의 profile sample image 3개를 frontend asset으로 추가했습니다.
  - 수정된 `sample/reaction_predictor/img1.png`를 `oa_profile_tri_bromo.png` asset에 재반영했습니다.

- `frontend/src/index.css`
  - Reaction Predictor profile image frame 스타일을 추가했습니다.
  - Reaction Predictor profile calculating 상태 스타일을 추가했습니다.
  - 전역 Table pagination UX 적용을 위해 Reaction Predictor 전용 pagination override를 제거했습니다.

- `frontend/src/pages/ReactionPredictor.tsx`
  - pagination page number에 셋 자리 comma 정규식 formatter를 적용해 공통 pagination UX와 맞췄습니다.
  - 계산 목록 Table을 `size="small"`로 변경해 MyBoard 그룹 상세 목록 기준과 맞췄습니다.

## MyBoard bookmark button UX update

- `frontend/src/pages/MyBoard.tsx`
  - 그룹 리스트 Row 고정 버튼 hover 시 icon 색/배경 변화가 적용되지 않도록 hover style을 제거했습니다.
  - Row 고정 버튼 active/focus-visible 상태의 border와 inset outline shadow를 제거했습니다.
  - 선택되지 않은 Row 고정 버튼 icon 색상을 더 연한 `colorTextQuaternary`로 변경했습니다.
  - Ant Design Button 기본 hover/focus/active 배경이 Row 고정 버튼에 적용되지 않도록 `background: transparent !important` override를 추가했습니다.
  - Row 고정 버튼 내부 `my-board-bookmark-icon`이 hover/focus/active 상태에서 black color/background-color로 바뀌지 않도록 icon 상태별 color override를 추가했습니다.
  - 선택된 Row 고정 버튼의 active/hover/focus-visible background를 제거하고 icon primary 색상만 유지하도록 변경했습니다.
  - 그룹 리스트의 화합물 구조 컬럼 row 하단 여백을 줄이기 위해 구조 셀 vertical padding과 line-height를 보정했습니다.
  - 그룹 리스트 대표 구조 SVG 기준 크기를 `130 x 97.5`로 변경했습니다.
  - 그룹 리스트 구조만 보기 접힘 상태에서 대표 구조가 잘리지 않도록 structure-only column/panel width와 overflow를 조정했습니다.
  - 그룹 리스트 펼침 상태의 화합물 구조 컬럼 cell 좌우 padding을 줄였습니다.
  - 그룹 리스트 펼침 상태의 화합물 구조 컬럼 width/min-width 기준을 `156px`에서 `135px`로 변경해 SVG 이미지 폭에 맞췄습니다.
  - 그룹 상세 목록 테이블의 표시 row 높이를 현재 렌더링된 row 중 최대 높이로 통일하도록 측정/ref 기반 UX를 추가했습니다.
  - 세로로 회전된 구조 이미지에서 preview/copy 버튼이 이미지 중간에 보이지 않도록 overlay 위치 기준을 container 우측 하단으로 변경했습니다.
  - SAR에서 90도로 회전한 구조를 MyBoard 그룹 상세 목록에서 볼 때 회전 bounding box 기준으로 width/height가 작아지지 않고 원본 이미지 frame 크기를 유지하도록 수정했습니다.
  - MyBoard 그룹 상세 목록에서 회전된 SVG가 다음 row 영역을 침범하지 않도록 회전 bounding box 기준의 최소 표시 영역을 확보했습니다.
  - 그룹 상세 목록 row 높이 통일 로직이 설정 변경 후에도 누락되지 않도록 cell content 높이 기준으로 최대값을 재측정하고 모든 visible row에 즉시 적용하도록 보정했습니다.
  - MyBoard 그룹 리스트/그룹 상세 목록 row 선택을 기본 좌클릭 단일 선택으로 변경하고, `Ctrl`/`Cmd` + 좌클릭일 때만 다중 토글되도록 수정했습니다.
  - 그룹 상세 목록 table view의 구조 이미지 `Scale` 컨트롤을 SAR Table 설정 영역과 동일하게 라벨/테두리/배경이 있는 compact group으로 분리했습니다.

- `frontend/src/components/common/CompoundStructureView.tsx`
  - 구조 action overlay 위치 기준을 `frame` 또는 `container`로 선택할 수 있는 `actionOverlayAnchor` 옵션을 추가했습니다.

## SAR Table group structure UX update

- `frontend/src/pages/SarTable.tsx`
  - SAR Table 그룹 구조 영역의 대표 구조 SVG 기준 크기를 MyBoard 접힘 상태와 동일한 `130 x 97.5`로 변경했습니다.
  - SAR 그룹 구조 영역의 panel/column width를 MyBoard 접힘 UX 기준에 맞춰 `146px`/`138px`로 조정했습니다.
  - SAR 그룹 구조 영역에서 구조 이미지가 잘리지 않도록 table/card overflow와 line-height, row padding을 보정했습니다.
  - SAR 화합물 카드에서 회전 시 rotated bounding box 기준으로 scale이 줄어드는 로직을 제거하고, 현재 이미지 크기를 유지한 채 회전되도록 변경했습니다.
  - SAR 화합물 카드 구조 frame/view overflow를 visible로 보정해 회전된 SVG가 틀에 의해 잘리지 않도록 했습니다.
  - SAR 화합물 카드 구조 영역을 가로/세로 중 큰 값 기준의 정사각형 frame으로 감싸 회전 시 clipping을 줄였습니다.
  - SAR 화합물 카드 hover/selected 상태의 실제 border는 transparent로 유지하고, `::after` overlay outline 한 겹만 primary로 표시되도록 보정했습니다.
  - SAR 화합물 카드 설정의 확대/회전/겹침 컨트롤을 각각 라벨이 있는 compact group으로 분리해 기능 구분이 명확하도록 UX를 개선했습니다.
  - SAR 화합물 카드 겹침 설정 최대값을 `30`에서 `50`으로 확장했습니다.
  - SAR 화합물 카드 리스트 첫 번째 카드 선택 시 왼쪽 outline이 잘리지 않도록 리스트 좌우 padding과 outline inset을 보정했습니다.
  - SAR 화합물 카드 리스트의 마지막 카드 선택 시 우측 outline이 잘리지 않도록 내부 row padding을 스크롤 content에 포함시키고, 전역 card selected box-shadow를 SAR 카드에서 제거했습니다.
  - SAR 화합물 카드 내부 compound name 텍스트와 카드 하단 사이에 작은 여백을 추가했습니다.
  - SAR 하단 테이블의 2단/3단 multi-level header에서도 각 header row 하단 구분선이 표시되도록 header border override를 보정했습니다.
  - SAR 하단 테이블 multi-level header의 내부 depth 사이 가로선을 제거하고, header/body 경계에 닿는 셀에만 하단선을 적용해 3단 헤더의 시각적 복잡도를 줄였습니다.
  - SAR 하단 테이블 1단 header 그룹 구분 UX를 배경색 방식이 아닌 각 그룹 마지막 header path의 세로 라인 방식으로 변경했습니다.
  - SAR 하단 테이블의 마지막 표시 컬럼에는 그룹 구분 세로 라인이 표시되지 않도록 제외했습니다.
  - SAR 하단 테이블 가로 스크롤 시 fixed Compound 컬럼이 header/selected row와 겹쳐 보이지 않도록 fixed cell 배경, z-index, 경계선을 보정했습니다.
  - SAR 하단 테이블 fixed Compound 컬럼의 header/selected/hover 배경을 불투명 색으로 보정해 스크롤된 컬럼 텍스트가 비쳐 보이지 않도록 수정했습니다.
  - SAR 하단 테이블 fixed Compound 컬럼 경계선이 body 영역에는 표시되지 않도록 header scope로 제한했습니다.
  - SAR 페이지의 그룹 구조 테이블과 화합물 카드/하단 테이블 선택을 기본 좌클릭 단일 선택, `Ctrl`/`Cmd` + 좌클릭 다중 토글 방식으로 변경했습니다.

## Synthesis Board selection UX update

- `frontend/src/pages/SynthesisBoard.tsx`
  - 합성 보드 그룹 리스트와 합성 상세 목록 테이블 row 선택을 기본 좌클릭 단일 선택, `Ctrl`/`Cmd` + 좌클릭 다중 토글 방식으로 변경했습니다.
  - 합성 보드 그룹 다중 선택 시 선택된 그룹들의 상세 항목을 함께 표시하도록 상세 목록 필터 기준을 확장했습니다.

## Selection color UX update

- `frontend/src/index.css`
  - 전역 table selected row와 selected hover 배경 alpha를 낮춰 선택 색상이 더 연하게 보이도록 조정했습니다.

- `frontend/src/pages/SarTable.tsx`
  - SAR 화합물 카드와 fixed Compound 셀의 selected/hovered 배경 톤을 전역 선택 색상에 맞춰 더 연하게 조정했습니다.

## Patent Analysis List number format update

- `frontend/src/pages/PatentAnalysisList.tsx`
  - 셋 자리 comma 구분을 적용하는 정규식 기반 공통 숫자 formatter를 추가했습니다.
  - 특허 분석 리스트/구조 검색 목록의 row number, metric, 관련 특허 수, 상단 총 개수, pagination page label에 comma 포맷을 적용했습니다.
  - `ant-pagination-total-text`가 표시되지 않도록 pagination total text를 제거했습니다.
  - 기존 페이지별 pagination 여백 override를 전역 pagination UX 기준에 맞춰 정리했습니다.

- `AGENTS.md`
  - 프론트엔드 숫자 표시 기본 룰을 셋 자리 comma 정규식 포맷으로 추가했습니다.
  - 프론트엔드 날짜 표시 기본 룰을 `YYYY.mm.dd` 정규식 포맷으로 추가했습니다.
  - Ant Design Table pagination 기본 UX를 MyBoard 그룹 상세 목록 기준으로 정의했습니다.

- `docs/UI_STANDARD_GUIDE.md`
  - 숫자/날짜 화면 표시 포맷 표준을 UI 가이드에 추가했습니다.
  - Table pagination 표준 UX를 UI 가이드에 추가했습니다.

- `frontend/src/index.css`
  - Ant Design Table pagination 전역 스타일을 MyBoard 그룹 상세 목록 기준으로 맞췄습니다.
  - pagination total text 숨김, 하단 우측 정렬, page item auto width, page number nowrap, active page primary 색상, 24px 높이/32px 최소 폭, pill radius, size changer 여백을 전역 적용했습니다.

- `frontend/src/pages/SynthesisBoard.tsx`
  - 합성 상세 목록 pagination을 MyBoard 기본 page size 옵션/size changer UX로 맞췄습니다.

- `frontend/src/pages/PatentAnalysisDetail.tsx`
  - Raw data table pagination 위치를 `bottomCenter`에서 `bottomRight`로 변경하고, 기본 page size 옵션을 추가했습니다.

## Frontend date display format update

- `frontend/src/utils/displayFormat.ts`
  - 셋 자리 comma 숫자 formatter와 `YYYY.mm.dd` 날짜 표시 formatter를 공통 유틸로 추가했습니다.
  - 날짜 formatter는 `YYYY-MM-DD`, `YYYY/MM/DD`, `YYYY.MM.DD`, `YYYY.MM.DD.`, `YY.MM.DD`, `YY.MM.DD HH:mm` 입력을 화면 표시용 `YYYY.mm.dd` 형식으로 정규식 변환합니다.

- `frontend/src/pages/MyBoard.tsx`
  - 그룹/상세 목록의 날짜 컬럼과 RangePicker 표시 형식에 공통 날짜 formatter를 적용했습니다.

- `frontend/src/pages/SynthesisBoard.tsx`
  - 그룹/상세 목록의 날짜 컬럼과 RangePicker 표시 형식에 공통 날짜 formatter를 적용했습니다.

- `frontend/src/pages/ReactionPredictor.tsx`
  - Start Date/End Date 컬럼에 공통 날짜 formatter를 적용했습니다.

- `frontend/src/pages/PatentAnalysisList.tsx`
  - 특허 목록/구조 검색 하위 특허 목록의 출판일과 기간 필터 tag에 공통 날짜 formatter를 적용했습니다.
  - 직접설정 RangePicker 표시 형식을 `YYYY.MM.DD`로 변경했습니다.

- `frontend/src/pages/PatentAnalysisDetail.tsx`
  - 상세 헤더의 출판일에 공통 날짜 formatter를 적용했습니다.

- `frontend/src/pages/PatentInsight.tsx`, `frontend/src/pages/SarTable.tsx`
  - RangePicker 표시 형식을 `YYYY.MM.DD`로 변경했습니다.

- `frontend/src/pages/Dashboard.tsx`, `frontend/src/pages/DevelopmentStatus.tsx`
  - 하드코딩된 날짜 표시값에 공통 날짜 formatter를 적용했습니다.
  - DevelopmentStatus의 최근 업데이트 문장 안에 포함된 날짜도 공통 날짜 formatter로 변환했습니다.

## Patent analysis detail raw data click performance update

- `frontend/src/hooks/usePatentPdfViewer.ts`
  - Raw data 카드/테이블 클릭 시 PDF `goToPage`/`scrollTo` 호출을 클릭 이벤트 내부에서 즉시 실행하지 않고 다음 animation frame으로 지연했습니다.
  - 연속 클릭 시 이전 예약된 PDF 이동 frame을 취소해 불필요한 PDF viewer 스크롤 작업을 줄였습니다.
  - 이미 비어 있는 highlight state를 다시 비우는 state update를 줄여 카드 클릭 시 재렌더 부담을 낮췄습니다.

- `frontend/src/pages/PatentAnalysisDetail.tsx`
  - Raw Data/Clean Data table pagination을 공통 UX 기준인 기본 10개, page size 옵션 `[10, 30, 50, 100]`으로 변경했습니다.
  - Raw Data/Clean Data card view에도 pagination을 추가해 현재 페이지의 카드만 렌더링하도록 변경했습니다.
  - Raw Data/Clean Data card view의 pagination 위치가 카드 높이에 따라 달라지지 않도록 공통 card view wrapper 하단 우측에 고정되게 정리했습니다.
  - Raw Data table의 fixed left 컬럼 배경/z-index/경계선을 보강하고, fixed 컬럼 구간을 연속화해 가로 스크롤 시 이동 컬럼 내용과 겹치지 않도록 수정했습니다.
  - fixed column 보강 스타일이 실제 Raw Data/Clean Data 탭 wrapper에 적용되도록 `raw-data-tab-content` class를 추가하고, fixed/non-fixed cell의 z-index를 명확히 분리했습니다.

- `frontend/src/index.css`
  - card view의 standalone Pagination에도 table pagination과 동일한 공통 UX 스타일을 적용할 수 있도록 `.v-common-pagination` 스타일을 추가했습니다.
  - card view pagination의 page item과 page size select 높이가 모두 24px로 맞도록 select wrapper 높이와 정렬을 보강했습니다.

## Patent analysis scrollbar theme update

- `frontend/src/pages/PatentAnalysisList.tsx`, `frontend/src/pages/PatentAnalysisDetail.tsx`
  - 특허 분석 리스트/상세 페이지 루트에 scrollbar 테마 적용 범위를 위한 class를 추가했습니다.

- `frontend/src/index.css`
  - 특허 분석 리스트/상세의 page, table body/content, raw data tab, tab pane, PDF viewer, thumbnail/sidebar 계열 스크롤에 다크/라이트 모드 scrollbar 색상 변수를 적용했습니다.
  - Patent Insight scrollbar도 같은 변수 기반으로 정리해 특허 분석 계열 페이지의 스크롤 테마를 일관화했습니다.

## Patent analysis raw data column order update

- `frontend/src/pages/PatentAnalysisDetail.tsx`
  - Raw Data 탭 테이블에서 `Example No.` 컬럼이 `Scaffold Rank` 컬럼보다 앞에 표시되도록 순서를 변경했습니다.
  - `Scaffold Rank` 컬럼의 left fixed 설정을 제거해 가로 스크롤 시 고정되지 않도록 수정했습니다.

## Frontend build error fix

- `frontend/src/types/external-modules.d.ts`
  - Reaction Predictor PNG asset import를 TypeScript가 인식할 수 있도록 `*.png` 모듈 선언을 추가했습니다.

- `frontend/src/services/patentInsightApi.ts`
  - Patent Insight heatmap normalize 함수의 map 반환 타입을 명시해 `null` 필터링 type predicate가 strict build에서 통과하도록 수정했습니다.

## Frontend datetime display format update

- `frontend/src/utils/displayFormat.ts`
  - `formatDisplayDate`가 날짜+시간 값을 `YYYY.mm.dd HH:MM` 형식으로 정규화하도록 추가했습니다.
  - 초, millisecond, `Z`/offset timezone이 포함된 ISO 형태는 화면 표시에서 분 단위까지만 남기도록 처리했습니다.

- `AGENTS.md`, `docs/UI_STANDARD_GUIDE.md`
  - 날짜+시간 화면 표시 표준과 예시를 `YYYY.mm.dd HH:MM` 기준으로 갱신했습니다.

## Patent Insight target filter default update

- `frontend/src/pages/PatentInsight.tsx`
  - 상세 필터 영역의 `Targets` 기본 수치를 20으로 변경했습니다.
  - `Targets` 입력값이 비어 있을 때도 20으로 복구되도록 fallback 값을 맞췄습니다.

## Reaction Predictor profile asset SVG conversion

- `frontend/src/assets/reaction_predictor/`
  - OA profile PNG 3개를 동일한 `430x360` 크기와 `viewBox`를 가진 SVG 파일로 변환했습니다.
  - 원본 분자 구조와 energy box가 달라지지 않도록 원본 PNG를 SVG 내부에 임베드하는 방식으로 조정했습니다.
  - 위쪽 여백을 추가하고 원본 이미지 레이어를 아래로 내려 구조가 SVG 영역 중앙에 더 가깝게 배치되도록 조정했습니다.
  - 기존 legend 영역은 SVG 레이어에서 덮고, 세 SVG의 `major`/`minor` 범례 텍스트와 원형 marker 좌표를 동일하게 정렬했습니다.
  - SVG 전환 후 기존 PNG asset 3개는 제거했습니다.
  - `oa_profile_022.svg`는 구조 위쪽 여백을 더 확보하고, 구조와 `major`/`minor` 범례 사이 간격이 줄어들도록 원본 이미지 레이어를 `y=60`으로 추가 이동했습니다.

- `frontend/src/pages/ReactionPredictor.tsx`, `frontend/src/types/external-modules.d.ts`
  - Reaction Predictor profile image import를 SVG asset으로 변경했습니다.
  - 일반 `*.svg` URL import 타입 선언을 추가했습니다.
