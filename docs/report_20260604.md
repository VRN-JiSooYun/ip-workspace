# 2026-06-04 작업 보고서

## SAR Table 화합물 설정 영역 조정

- `frontend/src/pages/SarTable.tsx`
  - SAR 페이지 화합물 설정 영역의 `Scale`, `Rotate`, `Overlap` 그룹 사이 간격을 `10px`로 조정했습니다.
  - Overlap 증가 버튼 아이콘을 `arrow-merge.svg`로 변경했습니다.
  - Overlap 감소 버튼 아이콘을 `arrow-divide.svg`로 변경했습니다.
  - SVG 아이콘이 다크 모드와 disabled 상태에서도 구분되도록 표시 스타일을 추가했습니다.
  - Overlap 버튼 내부 SVG 아이콘 크기를 키워 버튼 안에서 더 잘 보이도록 조정했습니다.
  - SAR 페이지 본문, 화합물 카드 리스트, SAR 테이블 스크롤바에 다크/라이트 모드별 thumb/track 색상을 적용했습니다.
  - Overlap 설정 그룹과 화합물 카드 보기 모드 버튼 사이 간격을 `15px`로 조정했습니다.
  - SAR 하단 table 영역 body row 높이를 `34px`로 맞췄습니다.
  - SAR 하단 table의 `C` color scale 토글을 버튼형 UI로 변경하고 ON/OFF 아이콘, tooltip, pressed 상태, 활성 card accent를 추가했습니다.
  - Color scale 적용 시 cell 내부 padding을 row 높이에 맞춰 줄여 토글 전후 row 높이가 흔들리지 않도록 조정했습니다.
  - SAR 화합물 이미지 더블클릭으로 pin 고정/해제하는 mock 상태 기능을 추가했습니다.
  - Pin 고정된 화합물 카드는 리스트 최좌측으로 정렬되고, 좌우 스크롤 시 sticky left로 고정되며 hover와 같은 primary 테두리/배경 및 pin badge를 표시합니다.
  - Pin 고정 상태는 하단 SAR table에도 연동되어 pinned row가 최상단으로 정렬되고 hover와 같은 배경/primary accent로 표시됩니다.
  - Pin 고정 카드와 가로 스크롤 카드의 compound name 텍스트가 겹치지 않도록 pinned 카드와 텍스트 영역을 불투명 배경/상위 z-index로 보정했습니다.
  - Pin 고정 카드 하단 compound name 영역에도 primary outline이 보이도록 outline layer와 텍스트 배경 inset을 보정했습니다.
  - Pin 고정 카드 사이 gap 영역에 불투명 마스크를 추가해 뒤에서 가로 스크롤되는 카드가 보이지 않도록 수정했습니다.

## My Board / SAR Table 이동 버튼 정렬

- `frontend/src/pages/MyBoard.tsx`
  - 그룹 상세 목록 헤더에 있던 `SAR Table로 보기` 버튼을 상단 검색 카드 우측 영역으로 이동했습니다.
  - 버튼명을 `SAR table`로 변경했습니다.
  - 버튼 색상은 기존 SAR 이동 버튼과 동일한 primary 색상을 유지했습니다.
  - `SAR table` 버튼의 우측 이동 아이콘을 lucide `ArrowRight`로 변경해 SAR Table의 `돌아가기` 좌측 아이콘과 계열을 통일했습니다.
  - `SAR table` 버튼 아이콘을 텍스트 우측에 배치했습니다.
  - `SAR table` 버튼과 SAR Table의 `돌아가기` 버튼 폭 기준을 `117px`로 맞췄습니다.
- `frontend/src/pages/SarTable.tsx`
  - 상단 검색 카드 우측의 `돌아가기` 버튼을 My Board의 SAR 이동 버튼과 같은 primary 색상으로 통일했습니다.

## 좌측 메뉴 구조 개편

- `frontend/src/components/layout/MainLayout.tsx`
  - 좌측 메뉴를 `Dashboard`, `Design`, `Synthesis`, `Documents`, `Compounds`, `Tools`, `통합검색` 순서로 재구성했습니다.
  - `Design`은 기존 My Board 역할로 `/design`에 연결했습니다.
  - `Synthesis`는 합성팀 보드 역할로 `/synthesis`에 연결했습니다.
  - `Documents` 하위 구성은 기존 Patents/Papers/Conferences 구조를 유지했습니다.
  - `Compounds` 하위 구성을 `Search`, `Chemical space`, `Clustering`으로 변경했습니다.
  - `Tools` 하위에 `Reaction Predictor`를 배치했습니다.
  - 하단 메뉴의 `수리응용2팀 서비스 개발 진행 현황`, `문의하기`는 유지했습니다.
- `frontend/src/App.tsx`
  - `/design`, `/synthesis`, `/compounds/search` 라우트를 추가했습니다.
- `frontend/src/pages/MyBoard.tsx`, `frontend/src/pages/SynthesisBoard.tsx`, `frontend/src/pages/SarTable.tsx`
  - 새 메뉴 구조에 맞춰 breadcrumb를 `Design`/`Synthesis` 기준으로 조정했습니다.
