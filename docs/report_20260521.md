# 작업 보고서 2026-05-21


---

## global table style

# Global Table Style Update

## Date
- 2026-05-21

## Scope
- Promoted shared table styling to `frontend/src/index.css`.

## Changes
- Added global table header background variables for light and dark themes.
- Applied the darker table header background to all Ant Design tables.
- Center-aligned all table header text globally.
- Standardized table body font size and vertical alignment globally.
- Added reusable responsive text-column classes, including support for MyBoard's responsive text columns.
- Removed duplicated MyBoard-local table header CSS so MyBoard uses the shared table baseline.
- Added matching Ant Design Table component tokens for `headerBg` and `headerColor`.
- Updated page-local table header overrides to use the shared `--table-header-bg` variable instead of transparent or lighter local backgrounds.

## Verification
- No build or dev server was run, per repository instruction that builds and execution are handled by the user.


---

## sar table compound card spacing

# SAR Table 화합물 카드 좌우 간격 조정

## 작업 일자
- 2026-05-21

## 요청 사항
- SAR Table 페이지의 화합물 영역에서 카드 간 좌/우 여백 줄이기

## 변경 내용
- `frontend/src/pages/SarTable.tsx`
  - 기본 카드 리스트 간격을 `24px`에서 `12px`로 줄였습니다.
  - 2줄 카드 모드의 좌/우 컬럼 간격을 `14px`에서 `10px`로 줄였습니다.
  - 2줄 카드 모드의 상/하 간격은 기존과 동일하게 `14px`로 유지했습니다.
  - 카드 크기와 내부 패딩은 유지했습니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## chemdraw korean keyboard bridge

# ChemDraw 한글 키보드 원자기호 입력 보정

## 작업 일자
- 2026-05-21

## 요청 사항
- ChemDraw에서 키보드가 한글 상태일 때 `N`, `O`, `H` 등 원자기호 입력이 되지 않는 문제 개선
- 한글 입력 시 같은 키 위치의 영문으로 자동 변환되어 입력되도록 처리

## 변경 내용
- `frontend/src/utils/chemdrawKeyboard.ts`
  - 2벌식 한글 자모를 같은 키 위치의 영문으로 매핑하는 ChemDraw 전용 keyboard bridge를 추가했습니다.
  - ChemDraw 컨테이너 내부 이벤트에 한해서 `keydown`, `beforeinput`, `compositionend`를 capture 단계에서 감지합니다.
  - `ㅜ -> n`, `ㅐ -> o`, `ㅗ -> h` 등 한글 자모 입력을 영문 keyboard event로 재전달합니다.
- `frontend/src/components/common/ChemDrawModal.tsx`
  - ChemDraw modal editor 준비 후 keyboard bridge를 설치하도록 추가했습니다.
- `frontend/src/components/common/ChemDrawEditor.tsx`
  - MyBoard 디자인 등록 등 embedded ChemDraw editor에도 동일 bridge를 적용했습니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## sidebar dropdown build fix

# Sidebar Mini Dropdown Placement 빌드 오류 수정

## 작업 일자
- 2026-05-21

## 요청 사항
- Docker build 중 `Dropdown`의 `placement="rightTop"` 타입 오류 수정

## 변경 내용
- `frontend/src/components/layout/MainLayout.tsx`
  - Ant Design `Dropdown` 타입에서 지원하지 않는 `rightTop` 값을 제거했습니다.
  - mini sidebar dropdown placement를 지원 값인 `topLeft`로 변경해 TypeScript 빌드 오류를 수정했습니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## myboard detail structure svg fill

# MyBoard 그룹 상세 목록 구조 SVG 채움 보정

## 작업 일자
- 2026-05-21

## 요청 사항
- 그룹 상세 목록 `화합물 구조` 컬럼 내부 SVG 이미지가 프레임을 꽉 채우도록 수정

## 변경 내용
- `frontend/src/pages/MyBoard.tsx`
  - 기존 `.my-board-structure-svg svg` 제한값 `max-width: 92px`, `max-height: 54px`를 제거했습니다.
  - SVG가 구조 프레임 안에서 `width: 100%`, `height: 100%`로 렌더링되도록 변경했습니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## myboard detail structure image size

# MyBoard 그룹 상세 목록 화합물 구조 이미지 확대

## 작업 일자
- 2026-05-21

## 요청 사항
- 그룹 상세 목록의 `화합물 구조` 컬럼 이미지를 약 2배 확대

## 변경 내용
- `frontend/src/pages/MyBoard.tsx`
  - 그룹 상세 목록 `화합물 구조` 컬럼 width를 `132px`에서 `240px`로 확대했습니다.
  - 구조 이미지 프레임을 `100x60px`에서 `200x120px`로 확대했습니다.
  - 구조 이미지가 없는 경우의 placeholder 아이콘도 `20px`에서 `40px`로 확대했습니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## myboard detail group number title rename

# MyBoard 그룹 상세 목록 그룹 번호 헤더명 변경

## 작업 일자
- 2026-05-21

## 요청 사항
- 그룹 상세 목록의 `그룹 번호` 컬럼 이름을 `그룹`으로 수정

## 변경 내용
- `frontend/src/pages/MyBoard.tsx`
  - 그룹 상세 목록 컬럼 정의에서 `그룹 번호` 컬럼의 표시 title을 `그룹`으로 변경했습니다.
  - 컬럼 key와 데이터 렌더링은 기존 `groupOrder` 및 `G1`, `G2` 표시 방식을 유지했습니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## myboard detail remove group column

# MyBoard 그룹 상세 목록 그룹 컬럼 삭제

## 작업 일자
- 2026-05-21

## 요청 사항
- 그룹 상세 목록에서 `그룹` 컬럼 삭제

## 변경 내용
- `frontend/src/pages/MyBoard.tsx`
  - 그룹 상세 목록 기본 컬럼 목록에서 `그룹` 컬럼을 제거했습니다.
  - `allColumnsMap`의 `그룹` 컬럼 정의를 삭제했습니다.
  - 반응형 텍스트 컬럼 대상에서 `groupId`를 제거했습니다.
  - `그룹 번호` 컬럼은 유지했습니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## myboard group list group number column

# MyBoard 그룹 리스트 그룹 번호 컬럼 추가

## 작업 일자
- 2026-05-21

## 요청 사항
- 그룹 상세 목록에 있는 `그룹 번호` 컬럼을 그룹 리스트의 `개수`와 `공유` 컬럼 사이로 복사

## 변경 내용
- `frontend/src/pages/MyBoard.tsx`
  - 그룹 리스트 컬럼에 `그룹 번호` 컬럼을 추가했습니다.
  - 위치는 `개수` 컬럼과 `공유` 컬럼 사이로 배치했습니다.
  - 그룹 상세 목록의 `그룹 번호`와 동일하게 선택된 그룹 순서 기준 `G1`, `G2` 형식으로 렌더링합니다.
  - 그룹 리스트 자동 맞춤 폭 계산에 새 `그룹 번호` 컬럼 폭을 반영했습니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## myboard group title content width

# MyBoard 그룹 리스트 Title content 기반 폭 계산

## 작업 일자
- 2026-05-21

## 요청 사항
- split 더블클릭 자동 조정 시 `Title`을 고정 최대폭으로 두지 않고 content 텍스트 길이에 맞춰 줄어들게 개선

## 변경 내용
- `frontend/src/pages/MyBoard.tsx`
  - 그룹명 텍스트의 한글/영문/공백 문자 폭을 추정하는 계산 함수를 추가했습니다.
  - `Title` 컬럼 폭을 고정 최대값이 아니라 현재 표시 중인 그룹명 content 폭 기준으로 산출하도록 변경했습니다.
  - 사용자가 split을 좁게 조정한 경우에는 사용 가능한 패널 폭 안에서 `Title` 폭이 줄어들도록 했습니다.
  - split 더블클릭 자동 맞춤도 같은 content 기반 `Title` 폭을 사용하도록 통일했습니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## myboard split autofit title tuning

# MyBoard split 자동 맞춤 Title 폭 조정

## 작업 일자
- 2026-05-21

## 요청 사항
- split 더블클릭 자동 조정 시 왼쪽 그룹 리스트의 `Title` 컬럼이 너무 길어져 가로 스크롤이 생기는 현상 개선

## 변경 내용
- `frontend/src/pages/MyBoard.tsx`
  - 그룹 리스트 `Title` 컬럼 최대 폭을 `420px`에서 `320px`로 줄였습니다.
  - 자동 맞춤 계산의 그룹명 문자당 예상 폭과 여유 padding을 줄였습니다.
  - 더블클릭 자동 맞춤 시 왼쪽 영역이 데이터 길이에 맞되 과도하게 넓어지지 않도록 조정했습니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## myboard split double click autofit

# MyBoard split 더블클릭 그룹 리스트 자동 맞춤

## 작업 일자
- 2026-05-21

## 요청 사항
- My Board 페이지의 좌/우 split 조절 영역을 더블 클릭하면 그룹 리스트 영역이 테이블 데이터 길이에 맞게 자동 조정되도록 기능 추가

## 변경 내용
- `frontend/src/pages/MyBoard.tsx`
  - 그룹 리스트에 표시 중인 데이터의 그룹명 길이를 기준으로 필요한 `Title` 컬럼 폭을 계산했습니다.
  - 그룹 리스트의 고정 컬럼 폭과 계산된 `Title` 폭을 합산해 필요한 왼쪽 패널 폭을 산출합니다.
  - split 핸들 더블클릭 시 산출한 폭을 split ratio로 변환해 왼쪽 그룹 리스트 영역을 자동 조정하도록 연결했습니다.
  - 기존 기본 50% 리셋 더블클릭 동작은 자동 맞춤 동작으로 대체했습니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## myboard detail header width

# MyBoard 그룹 상세 목록 헤더 폭 조정

## 작업 일자
- 2026-05-21

## 요청 사항
- My Board 페이지 그룹 상세 목록에서 header가 2줄로 나오는 컬럼을 1줄로 보이게 width 늘리기

## 변경 내용
- `frontend/src/pages/MyBoard.tsx`
  - 그룹 상세 목록의 긴 header 컬럼 width를 확대했습니다.
  - 반응형 텍스트 컬럼 최소/최대 폭을 `180/360px`에서 `220/420px`로 늘렸습니다.
  - 반응형 helper가 개별 컬럼 width보다 작게 덮어쓰지 않도록, 개별 width와 반응형 계산값 중 큰 값을 사용하도록 수정했습니다.
  - 테이블은 기존처럼 가로 스크롤 계산값을 사용하므로 화면이 좁을 때 컬럼이 줄바꿈되기보다 가로 스크롤로 유지됩니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## myboard group share status context menu

# MyBoard 그룹 공유 상태 및 우클릭 메뉴 수정

## 작업 일자
- 2026-05-21

## 요청 사항
- My Board 페이지 그룹 리스트의 공유 상태를 3개로 수정
  - 공유 하는중: `frontend/src/assets/svg/share-forward-fill.svg`
  - 공유 받는중: `frontend/src/assets/svg/share.svg`
  - 공유 안할 때: 빈값
- 공유 하는중/받는중 아이콘 색상 구분
- 공유 안할 때 row 우클릭 시 드롭다운 팝업 호출

## 변경 내용
- `frontend/src/mocks/compounds.ts`
  - `CompoundGroup.shareStatus`를 `공유 하는중`, `공유 받는중`, `공유 안함` 3상태로 변경했습니다.
  - mock 그룹 데이터에 3상태가 모두 보이도록 반영했습니다.
- `frontend/src/pages/MyBoard.tsx`
  - 공유 컬럼에서 `공유 하는중`은 `share-forward-fill.svg`, `공유 받는중`은 `share.svg`를 mask icon으로 표시하도록 변경했습니다.
  - 두 공유 상태 아이콘에 서로 다른 색상을 적용했습니다.
  - `공유 안함` 상태는 공유 컬럼을 빈값으로 렌더링합니다.
  - `공유 안함` row 우클릭 시 `그룹 복사`, `그룹명 변경`, `삭제` 메뉴가 있는 드롭다운 팝업을 표시하도록 추가했습니다.
  - 우클릭 좌표에 배치되는 Dropdown anchor를 `display: block`인 1px 고정 요소로 보강해 메뉴 위치 계산이 정상 동작하도록 수정했습니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## patent filter recent projects layout

# 특허 분석 상세 필터 Recent Projects 위치 조정

## 작업 일자
- 2026-05-21

## 요청 사항
- 특허 분석 페이지 상세 필터 영역의 `Recent Projects`를 `분석 상태` 우측으로 이동

## 변경 내용
- `frontend/src/pages/PatentAnalysisList.tsx`
  - 상세 필터 첫 줄을 `특허청`, `분석 상태`, `Recent Projects` 3개 컬럼으로 재배치했습니다.
  - `특허청`과 `분석 상태`는 각각 `span=6`, `Recent Projects`는 `span=12`로 조정했습니다.
  - 기존 compact 필터 여백과 토글 동작은 유지했습니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## compact detail filter spacing

# 상세 필터 영역 여백 축소

## 작업 일자
- 2026-05-21

## 요청 사항
- 상세 필터 영역이 있는 모든 페이지의 위/아래 여백 줄이기
- 토글 버튼과 필터 영역 사이의 위/아래 여백을 절반 수준으로 줄이기
- 필터 영역 전체의 위/아래 여백 줄이기

## 변경 내용
- `frontend/src/index.css`
  - `compact-filter-card`, `compact-filter-panel` 공통 클래스를 추가했습니다.
- `frontend/src/pages/MyBoard.tsx`
- `frontend/src/pages/SynthesisBoard.tsx`
- `frontend/src/pages/SarTable.tsx`
- `frontend/src/pages/PatentAnalysisList.tsx`
  - 상세 필터 카드 body padding을 `12px 16px` 기준으로 축소했습니다.
  - 필터 토글 row의 세로 gutter를 `16px`에서 `8px`로 줄였습니다.
  - 토글 row와 필터 패널 사이 간격을 `24px`에서 `12px`로 줄였습니다.
  - 필터 패널 내부 padding을 `20px`에서 `12px 14px`로 줄였습니다.
  - 필터 항목 세로 gutter를 `24px`에서 `12px`로 줄였습니다.
  - label과 토글 버튼 그룹 사이 간격을 `8px` 또는 `12px`에서 `4px`로 줄였습니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## myboard table ui

# MyBoard Table UI Update

## Date
- 2026-05-21

## Scope
- Updated My Board table presentation in `frontend/src/pages/MyBoard.tsx`.

## Changes
- Scoped table header styling to MyBoard tables.
- Darkened the table header background slightly in light and dark themes.
- Center-aligned table header text.
- Added fixed widths for non-long columns in the group table, including the share action column.
- Made long text columns in the detail table responsive based on the current viewport and split panel width.
- Kept non-long detail columns on explicit fixed widths so they do not collapse when long text columns are visible.
- Increased responsive text column minimum width and switched table horizontal scroll to calculated numeric widths so columns do not collapse or disappear on narrow screens.

## Verification
- No build or dev server was run, per repository instruction that builds and execution are handled by the user.


---

## patent detail responsive split

# 특허 분석 상세 페이지 반응형 split 레이아웃

## 작업 일자
- 2026-05-21

## 요청 사항
- 특허 분석 상세 페이지에서 좌/우 영역 폭이 줄어들면 PDF 영역과 분석 영역이 상/하 배치되도록 반응형 웹 디자인 적용

## 변경 내용
- `frontend/src/pages/PatentAnalysisDetail.tsx`
  - split 레이아웃 전환 기준을 브라우저 전체 폭이 아닌 실제 split 컨테이너 폭 기준으로 변경했습니다.
  - 실제 작업 영역 폭이 `1280px` 이하로 줄어들면 좌/우 배치를 상/하 배치로 전환합니다.
  - `ResizeObserver`로 split 컨테이너 폭을 추적해 사이드바, 패딩, 레이아웃 변화에도 반응하도록 했습니다.
  - 스택 모드의 PDF 영역 높이를 `clamp(520px, 72vh, 760px)`로 지정해 너무 작아지지 않도록 했습니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## patent number column width

# 특허 분석 목록 특허 번호 컬럼 폭 조정

## 작업 일자
- 2026-05-21

## 요청 사항
- 특허 분석 페이지의 `특허 번호` 컬럼 width를 기존보다 약 1.5배 늘리기

## 변경 내용
- `frontend/src/pages/PatentAnalysisList.tsx`
  - `특허 번호` 컬럼의 `width`를 `180px`에서 `270px`로 조정했습니다.
  - 기존 중앙 정렬 및 공통 테이블 스타일은 유지했습니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## patent title column responsive

# 특허 분석 목록 제목 컬럼 반응형 폭 보정

## 작업 일자
- 2026-05-21

## 요청 사항
- 특허 분석 페이지 테이블의 `제목` 컬럼이 창 크기 축소 시 사라지는 현상 개선

## 변경 내용
- `frontend/src/pages/PatentAnalysisList.tsx`
  - `제목` 컬럼에 명시 폭 `480px`을 지정했습니다.
  - 테이블 `scroll.x`를 항상 `1370px`로 지정해, 화면이 좁아질 때 컬럼이 압축되어 사라지는 대신 가로 스크롤로 표시되도록 했습니다.
  - `tableLayout="fixed"`를 적용해 컬럼 폭 계산을 안정화했습니다.
  - `제목` 컬럼에 공통 반응형 텍스트 컬럼 클래스 `responsive-text-column`을 적용했습니다.

## 확인 사항
- 사용자 지침에 따라 빌드/실행은 수행하지 않았습니다.


---

## sidebar menu spacing

# Sidebar Menu Spacing Update

## Date
- 2026-05-21

## Scope
- Adjusted the left sidebar layout in `frontend/src/components/layout/MainLayout.tsx`.

## Changes
- Reduced the sidebar top padding and logo block bottom margin so the action buttons and menu start slightly higher.
- Reduced VORA and Medichem ELN button height, radius, and bottom margins.
- Reduced the divider bottom margin before the main navigation.
- Reduced Ant Design menu item and submenu title height from 48px to 42px, with tighter vertical spacing.
- Relaxed 2-depth and 3-depth menu indentation by lowering `inlineIndent` and nested submenu left offset.
- Fixed sidebar menu icon SVG dimensions at 22px so mini mode keeps the same icon size.
- Added mini mode-specific centering rules so selected 1-depth menu backgrounds and icons share the same 40px center axis.
- Changed the top sidebar action buttons to block-level flex buttons so mini mode horizontal auto margins center them correctly.
- Split mini mode menu icon wrapper and SVG sizing so icons remain visible while the selected background stays centered.
- Fixed mixed Ant Design icon rendering paths by keeping direct `svg.ant-menu-item-icon` elements at 22px while only wrapper elements use the 40px centering box.
- Narrowed mini mode overrides to direct collapsed root menu items only, so 2-depth and deeper submenu labels remain visible and horizontal overflow is avoided.
- Added a sidebar popup menu class for collapsed submenu overlays and explicitly restored popup label and arrow visibility for nested submenu levels.
- Wrapped every sidebar menu icon with a stable `sidebar-menu-icon` element so mini mode no longer depends on Ant Design's mixed direct-SVG versus wrapped icon rendering.
- Replaced the mini sidebar's collapsed Ant Design `Menu` rendering with a custom icon rail and Ant Design `Dropdown` overlays for nested 2-depth and 3-depth menus.
- Added explicit mini menu labels so leaf icon hover tooltips and accessibility labels use the real menu names instead of route-style keys.
- Added parent menu name headers to mini dropdown overlays so submenu icons still show their 1-depth menu name while preserving full 2-depth and 3-depth labels.
- Corrected mini popup styling from Ant Design `Menu` selectors to `Dropdown` selectors so 2-depth and 3-depth labels render at full width instead of collapsing to first letters.
- Wrapped mini dropdown labels in `mini-dropdown-label` spans and forced dropdown title content to use `max-content` sizing to prevent first-letter clipping.
- Cleared native title text on mini dropdown labels because 2-depth and 3-depth names are now fully visible without tooltip help.
- Cleared Ant Design dropdown item-level `title` values so native browser tooltips do not appear on 2-depth and 3-depth hover.
- Added mini-mode hover tooltips to the top VORA and Medichem ELN shortcut buttons.

## Verification
- No build or dev server was run, per repository instruction that builds and execution are handled by the user.


---

## table center columns

# Table Center Column Alignment

## Date
- 2026-05-21

## Scope
- Updated table cell alignment across MyBoard, SynthesisBoard, SAR Table, PatentAnalysisList, and PatentAnalysisDetail.

## Changes
- Added a reusable global `table-center-column` CSS class.
- Center-aligned date columns, tag/status columns, structure/image columns, compound/material identifier columns, design/number/rank columns, and chart/property columns where they are defined.
- Applied automatic center alignment to MyBoard's eligible columns through its column post-processing helper.
- Applied center alignment to SAR Table's dynamic nested metric columns through its column generation helper.
- Added explicit center alignment to patent list and patent detail rank/example/structure/action columns.

## Verification
- No build or dev server was run, per repository instruction that builds and execution are handled by the user.
