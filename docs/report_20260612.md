# 2026.06.12 작업 보고서

## 특허 분석 즐겨찾기 기본 폴더 중복 생성 방지

- 문제: 특허 분석 리스트에서 여러 즐겨찾기 추가 요청이 거의 동시에 들어오면, 각 요청이 동일한 폴더 트리 상태를 조회한 뒤 `myworkspace` 기본 폴더 생성을 중복 호출할 수 있었다.
- 원인: 기존 로직은 `GET-PATENT-LIST`로 폴더 트리를 조회하고 `myworkspace`/`ownerId` 폴더가 없으면 생성하는 체크는 있었지만, owner별 resolve/create 과정이 직렬화되어 있지 않았다.
- 수정: `backend/src/patent-analysis/patent-analysis.service.ts`에 owner별 `favoriteFolderResolveLocks`를 추가해 동일 owner의 기본 즐겨찾기 폴더 resolve/create 요청을 하나의 Promise로 공유하도록 했다.
- 추가 수정: helper/UI에서 폴더명이 `myworkspace/`처럼 trailing slash를 포함해 내려오는 경우도 기존 `myworkspace`로 인식하도록 폴더명 비교를 정규화했다.
- 추가 수정: 이미 중복 root가 있는 경우 `{ownerId}` 하위 폴더가 존재하는 `myworkspace` root를 우선 재사용하도록 했다.
- 추가 수정: resolve된 기본 즐겨찾기 folder id를 owner별 메모리 캐시에 저장해 같은 서버 인스턴스에서 반복 즐겨찾기 추가 시 폴더 트리 재조회/생성 경로를 다시 타지 않도록 했다.
- 추가 수정: 폴더 트리 확인 API를 원본 `patent_files` 화면과 동일하게 `GET-TARGET-LIST`로 변경했다. 기존 `GET-PATENT-LIST`는 특허 목록 조회 목적의 endpoint라 폴더 중복 체크 기준으로 쓰기 부적절할 수 있다.
- 추가 수정: `GET /api/patents/favorites` 조회 endpoint를 추가했다. 이 endpoint는 `GET-TARGET-LIST`의 기본 폴더 `patents[].publication_number`를 반환하며, 조회 중에는 폴더를 새로 생성하지 않는다.
- 추가 수정: 특허 분석 리스트 페이지 로드 시 즐겨찾기 publication number 목록을 Set으로 보관하고, 리스트 row 및 구조검색 하위 특허 row의 `isFavorite` 표시를 해당 Set 기준으로 보정한다.
- 영향 범위: `POST /api/patents/favorites`, `POST /api/patents/favorites/remove`, `POST /api/patents/favorites/share`, `GET /api/patents/my?favoriteOnly=true`에서 기본 폴더 resolve 시 동일 로직이 적용된다.

## 확인 필요

- 이미 생성된 중복 `myworkspace` 폴더는 이번 수정으로 자동 병합/삭제하지 않는다. 운영 데이터 정리는 별도 스크립트 또는 helper API 정책 확인 후 진행해야 한다.
- 컨테이너가 이전 빌드 코드로 실행 중이면 동일 문제가 계속 발생한다. 변경 반영 후 backend 컨테이너 재시작/재빌드가 필요하다.

## Patent Insight heatmap에서 특허 리스트 검색 연동

- `Target x Applicant heatmap` cell 클릭 시 특허 분석 리스트로 이동하도록 했다.
- Insight heatmap 데이터에 `applicant`가 있으면 x축을 Applicant로 사용하고, 클릭 시 `target + applicant + 현재 Insight 기간` 조건을 전달한다.
- `applicant`가 없는 구버전/mock 응답은 기존처럼 연도 x축을 사용하고, 클릭 시 `target + 해당 연도` 조건을 전달한다. Insight 화면의 Applicant 검색어가 있으면 함께 전달한다.
- 이동 URL은 `/patents/analysis?source=insight&target={target}&applicant={applicant}&dateFrom={YYYY-MM-DD}&dateTo={YYYY-MM-DD}` 형식을 사용한다.
- 특허 분석 리스트는 최초 진입 시 URL query를 읽어 검색조건을 자동 바인딩한다.
  - `target` → Target 필터
  - `applicant` → 출원인 검색어
  - `dateFrom/dateTo` → 직접설정 기간
- Query 기반 초기 상태는 리스트 조회 API 호출 전에 적용되므로, 이동 직후 해당 조건으로 목록이 조회된다.
- Heatmap cell click용 부가 데이터를 추가하면서 색상 기준 dimension이 흔들릴 수 있어 `visualMap.dimension=2`, `series.encode.value=2`를 명시했다.

## 특허 상세 Clean Data 테이블 컬럼 폭 조정

- Clean Data 탭 Table view의 컬럼 폭을 재조정했다.
  - 선택/pin/rank/Scaffold Group/Example Number/관리 컬럼은 더 촘촘하게 줄였다.
  - Structure, Scaffold, R-group 컬럼은 구조 이미지를 유지할 수 있도록 폭을 확보했다.
  - Bioactivity 동적 컬럼은 180px 기준으로 정리했다.
- SVG 이미지 프레임을 약간 키웠다.
  - Structure: 160px → 176px
  - Scaffold: 184px → 200px
  - R-group: 156px → 172px
- 테이블 내 구조 SVG의 최대 표시 비율을 92%에서 97%로 키워 이미지가 축소되어 보이지 않도록 했다.

## 특허 리스트 Recent Projects 옵션 변경

- 특허 리스트 검색 조건의 Recent Projects를 heatmap 주요 target 기준으로 변경했다.
- 적용 목록: EGFR, PD-1, PD-L1, KRAS, HER2, CD3, CD19, TNF, VEGF, BTK, AKT1, MET, FGFR3, PKMYT1, WEE1, VRK1, UBP1.

## 특허 리스트 Target 검색 조건 원본 동기화

- 원본 `patent_files`의 target 검색 payload는 `str#target`에 `"%s ilike '%%%s%%'"` 조건을 사용한다.
- 기존 backend target 필터는 `"%s='%s'"` 정확 일치라 `UBP1`처럼 target 값 안에 포함된 항목을 찾지 못할 수 있었다.
- Recent Projects 단독 target 검색은 backend 자동 target 필터를 통해 원본과 동일한 `ilike` 포함 검색을 사용한다.
- 특허번호/출원인 등 다른 검색조건과 조합되는 frontend 직접 `filter_dict` 경로의 target 조건은 정확 일치(`%s='%s'`)로 유지한다.

## 특허 상세 Raw/Clean Data Excel 다운로드

- backend에 `GET /api/patents/:publicationNumber/embodiments/excel` 다운로드 endpoint를 추가했다.
- helper API에는 원본과 동일하게 `DOWNLOAD-EMBODIMENTS-EXCEL` operation을 전달한다.
  - Raw Data: `bioactivity_type=bioactivity`
  - Clean Data: `bioactivity_type=modified_bioactivity`
  - 공통 payload: `file_extension=xlsx`, `filter_dict={}`, `ligand_filter_dict=[]`, `order_dict=[{"column_name":"ranking","order":"asc"},{"column_name":"ranking","order":"asc"}]`, `filter_group_conjunction_list=[]`
- 상세 페이지 Raw Data/Clean Data 탭 헤더에 Excel 다운로드 버튼을 추가했다.
- 동적 상세 route보다 구체적인 download/embodiments route가 먼저 매칭되도록 controller route 순서를 정리했다.
- frontend가 API URL을 anchor href로 직접 클릭하면 `VITE_API_URL=http://localhost:3000` 환경에서 cross-origin URL로 페이지 이동할 수 있어, Excel 다운로드를 `fetch -> Blob -> object URL` 방식으로 변경했다.
- `bioactivityType` query가 DTO whitelist 검증에서 거부되어 400이 발생하지 않도록 `PatentDetailQueryDto`에 optional 필드로 추가했다.

## 특허 상세 Clean Data 체크박스 컬럼 제거

- Clean Data 탭 Table view의 좌측 체크박스 컬럼을 제거했다.

## 특허 상세 Excel 다운로드 빈 데이터 UX

- Raw Data/Clean Data Excel 버튼은 다운로드할 row가 없으면 비활성화되도록 했다.
- 핸들러에서도 row count를 재확인해 빈 데이터 다운로드 요청을 막고 warning 메시지를 표시한다.
