# 작업 보고서 2026-05-20


---

## chemspace 3d mode toggle restore

# ChemSpace 3D Mode Toggle Restore

## Date
- 2026-05-20

## Scope
- Restored the 3D Mode toggle in ChemSpace visual controls.

## Changes
- Added a `3D Mode` control row with the `Rotate3d` icon and ON/OFF toggle.
- Disabled 3D Mode while 3Chart Mode is active.
- When 3D Mode is enabled, 3Chart Mode is automatically turned off to avoid conflicting chart render modes.
- Removed unused ChemSpace imports left from earlier UI iterations.

## Files
- `frontend/src/pages/ChemSpace.tsx`


---

## chemspace fullscreen three chart

# ChemSpace Fullscreen Three Chart Mode

## Date
- 2026-05-20

## Scope
- Fixed ChemSpace fullscreen rendering when 3Chart Mode is enabled.

## Changes
- Added `isThreeChartMode` handling inside the fullscreen overlay.
- Rendered the same three chart configuration in fullscreen instead of falling back to the single 2D chart.
- Replaced the fullscreen 3Chart `Row/Col` layout with a constrained CSS grid to prevent horizontal scrolling.
- Kept 3D fullscreen behavior unchanged when 3Chart Mode is off.

## Files
- `frontend/src/pages/ChemSpace.tsx`


---

## chemspace two chart mode

# Chemical Space Two Chart Mode

## Date
- 2026-05-20

## Scope
- Added a two-chart mode to the Chemical Space page.

## Changes
- Added `2Chart Mode` to Visual Controls.
- `2Chart Mode` renders only:
  - `Lipophilicity vs Polar Surface Area`
  - `Size vs Lipophilicity`
- Set `Size vs Lipophilicity` axis mapping to X: `molWt`, Y: `tpsa`.
- Kept `2Chart Mode`, `3Chart Mode`, and `3D Mode` mutually exclusive.
- Updated normal and fullscreen chart rendering to share the same multi-chart behavior.

## Files
- `frontend/src/pages/ChemSpace.tsx`


---

## full width layout stability

# Full Width Layout Stability

## Date
- 2026-05-20

## Scope
- Improved page layout stability around 1920px and below.

## Changes
- Removed the shared `1600px` max-width cap that was applied below 1920px.
- Unified shared side padding to `16px` across all viewport presets, including 1920px, 2560px, and 3200px breakpoints.
- Added `boxSizing: 'border-box'` to page-level containers that use shared side padding, preventing padding from increasing the effective page width and causing asymmetric clipping.

## Applied Pages
- Dashboard
- MyBoard
- SAR Table
- Synthesis Board
- ChemSpace / ChemSpace3D
- Patent Analysis List / Detail
- Development Status

## Files
- `frontend/src/config/patentAnalysisLayout.ts`
- `frontend/src/pages/Dashboard.tsx`
- `frontend/src/pages/MyBoard.tsx`
- `frontend/src/pages/SarTable.tsx`
- `frontend/src/pages/SynthesisBoard.tsx`
- `frontend/src/pages/PatentAnalysisList.tsx`
- `frontend/src/pages/PatentAnalysisDetail.tsx`
- `frontend/src/pages/DevelopmentStatus.tsx`


---

## global table hover unification

# Global Table Hover Unification

## Date
- 2026-05-20

## Scope
- Unified Ant Design table row hover and selected-row colors across all pages.

## Changes
- Added global CSS variables for table hover backgrounds:
  - `--table-row-hover-bg`
  - `--table-row-selected-bg`
  - `--table-row-selected-hover-bg`
- Added global Ant Table hover selectors in `frontend/src/index.css`.
- Set the Ant Design `Table.rowHoverBg` token in `frontend/src/App.tsx`.
- Updated page-level table hover overrides to reference the shared CSS variable.
- Standardized `row-selected`, `sar-row-selected`, `my-board-group-row-selected`, and `raw-data-row-active` selected states using the MyBoard color values.
- Aligned Patent Analysis table hover and active-row styles with the same shared colors.

## Files
- `frontend/src/index.css`
- `frontend/src/App.tsx`
- `frontend/src/pages/MyBoard.tsx`
- `frontend/src/pages/SarTable.tsx`
- `frontend/src/pages/SynthesisBoard.tsx`
- `frontend/src/pages/PatentAnalysisList.tsx`
- `frontend/src/pages/PatentAnalysisDetail.tsx`


---

## medichem eln sidebar button

# Medichem ELN Sidebar Button

## Date
- 2026-05-20

## Scope
- Added a `Medichem ELN` button below the VORA button in the left sidebar.

## Changes
- Added a second sidebar action button under VORA.
- Applied a light orange visual style for light mode and a muted orange style for dark mode.
- Matched the VORA button width and horizontal alignment values.
- Adjusted VORA bottom spacing so the two top action buttons sit as a pair above the main menu.
- Added a subtle divider below the action buttons to separate them from the main navigation menu.

## Note
- No click URL was added because the project does not currently define a Medichem ELN route or external URL.

## Files
- `frontend/src/components/layout/MainLayout.tsx`


---

## myboard detail table page size

# MyBoard Detail Table Page Size

## Date
- 2026-05-20

## Scope
- Updated MyBoard group detail table pagination.

## Changes
- Changed the default group detail table page size from 8 rows to 10 rows.

## Files
- `frontend/src/pages/MyBoard.tsx`


---

## myboard group list representative structure

# MyBoard Group List Representative Structure

## Date
- 2026-05-20

## Scope
- Added a representative compound structure column to the MyBoard group list table.

## Changes
- Added `화합물 구조` column before `Title` in the group list table.
- The column displays the structure SVG from the first compound row belonging to each group.
- Reused the existing structure preview modal behavior from the group detail table.
- Added a fallback Benzene icon when a group has no representative structure.

## Files
- `frontend/src/pages/MyBoard.tsx`


---

## myboard group list three stage collapse

# MyBoard Group List Three Stage Collapse

## Date
- 2026-05-20

## Scope
- Improved the MyBoard group list collapse UX with three states.

## Changes
- Replaced the previous open/closed group list state with:
  - `full`: current full group list table.
  - `structure`: compact group list showing only representative compound structures.
  - `hidden`: fully collapsed group list.
- The compact structure mode keeps group row selection available by clicking the representative structure row.
- Added controls to move from full to structure, structure to full, and structure to hidden.
- The detail panel expands when the group list is fully hidden.
- Matched the spacing between the compact structure-only group list and the group detail panel to the normal split-panel spacing.
- Kept the compact structure-only controls icon-only and increased button spacing for clearer separation in the narrow panel.
- Changed the compact structure-only column header from an icon to `화합물 구조` text.

## Files
- `frontend/src/pages/MyBoard.tsx`


---

## myboard group order column

# MyBoard Group Order Column

## Date
- 2026-05-20

## Scope
- Updated MyBoard group detail ordering and columns.

## Changes
- Sorted the group detail compound table by the order in which groups are clicked in the group list.
- Preserved the original compound order inside each selected group.
- Added a `그룹 번호` column immediately after `순번`.
- Displayed clicked group order as `G1`, `G2`, `G3`, etc.
- Added `그룹 번호` to the default visible column set.

## Files
- `frontend/src/pages/MyBoard.tsx`


---

## myboard mock detail data

# MyBoard Mock Detail Data

## Date
- 2026-05-20

## Scope
- Expanded mock compound detail data for MyBoard group selections.

## Changes
- Added generated mock detail rows for `FGFR 나의 디자인`.
- Added generated mock detail rows for `HER2 활성 증가`.
- Added additional generated mock detail rows for `cMET Tepotinib 변형`.
- Kept existing hand-authored cMET mock rows.
- Matched generated detail row counts to the group list counts:
  - `g1`: 11 rows
  - `g2`: 17 rows
  - `g3`: 7 rows

## Files
- `frontend/src/mocks/compounds.ts`


---

## myboard table hover contrast

# MyBoard Table Hover Contrast

## Date
- 2026-05-20

## Scope
- Improved table row hover contrast for pages under the My Board menu.

## Changes
- Applied a stronger primary-tinted hover background to MyBoard tables.
- Updated SAR Table row hover and selected-row hover backgrounds.
- Added matching hover styling to Synthesis Board tables.
- Kept dark mode colors stronger than the previous near-black hover while preserving selected-row distinction.

## Files
- `frontend/src/pages/MyBoard.tsx`
- `frontend/src/pages/SarTable.tsx`
- `frontend/src/pages/SynthesisBoard.tsx`


---

## sar table compound card keyboard navigation

# SAR Table Compound Card Keyboard Navigation

## Date
- 2026-05-20

## Scope
- Added keyboard navigation for selected compound cards in SAR Table.

## Changes
- Made the compound card list focusable.
- Added arrow key navigation for the selected card while the card list is focused.
- Added page-level arrow key handling when a compound card is selected, excluding text inputs, selects, pickers, segmented controls, and modals.
- In default single-row mode:
  - Left/Up moves to the previous card.
  - Right/Down moves to the next card.
- In two-row mode:
  - Left/Right moves to the immediately adjacent card.
  - Up/Down moves between rows using the visible column count.
- Added Home/End support for first/last card.
- Scrolls the newly selected card into view.
- Added `role="option"` and `aria-selected` to compound cards.
- Aligned the two-row grid placement with keyboard navigation to prevent left/right from skipping cards.
- Prevented duplicate keyboard handling between the focused card list and the page-level keydown listener.
- Removed focus-visible outlines from the compound card list and cards while preserving selected-card styling.
- Added bottom scroll breathing room to the SAR page and SAR table body so the last data row is not clipped when the compound card area uses two-row mode.

## Files
- `frontend/src/pages/SarTable.tsx`


---

## sar table compound card two rows

# SAR Table Compound Card Two Rows

## Date
- 2026-05-20

## Scope
- Added a second viewing mode for the compound card strip in SAR Table.

## Changes
- Added a `기본 / 2줄` segmented control above the compound cards.
- Preserved the existing single-row horizontal card mode as `기본`.
- Added a `2줄` mode using a two-row horizontal grid.
- Reduced card and structure preview height in `2줄` mode so more compounds are visible without making the section overly tall.
- Applied the existing SAR compound card hover/selected CSS class to the cards.
- Changed the compound area title icon to `BenzeneIcon`.
- Set the detailed search filter area to collapsed by default.
- Increased compound structure image bounds by about 1.5x while keeping them constrained to the card frame.
- Reduced card padding and compound name spacing so the structure frame can use more of each card.
- Increased the compound structure frame height in both default and two-row modes.

## Files
- `frontend/src/pages/SarTable.tsx`


---

## vora sidebar button alignment

# VORA Sidebar Button Alignment

## Date
- 2026-05-20

## Scope
- Updated the VORA external link button in the left sidebar.

## Changes
- Removed the VORA button icon.
- Applied a light purple visual style for light mode and a muted purple style for dark mode.
- Shifted the VORA button outline start position to align with the main menu icon start position.
- Kept a compact `V` label in mini sidebar mode so the button remains visible without using an icon.

## Files
- `frontend/src/components/layout/MainLayout.tsx`
