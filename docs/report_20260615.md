# 2026.06.15 작업 보고서

## MyBoard Canvas 상태 보존

- 그룹 상세 목록의 Canvas 탭에서 편집한 내용을 `MyBoard` 페이지 생명주기 동안 유지하도록 수정했습니다.
- `WhiteboardEditor`가 언마운트될 때 Fabric canvas JSON을 저장하고, 다시 마운트될 때 해당 JSON을 복원합니다.
- 그룹 리스트 선택 변경, Table 탭 이동, Tree 탭 이동으로는 canvas가 초기화되지 않습니다.
- 페이지를 새로 로드하거나 `MyBoard` 페이지를 떠난 뒤 다시 진입하면 새 페이지 인스턴스가 생성되어 기존처럼 초기 상태로 시작합니다.

## 변경 파일

- `frontend/src/components/board/WhiteboardEditor.tsx`
- `frontend/src/pages/MyBoard.tsx`

## 구조 이미지 복사 테마 반영

- 화합물 구조 이미지 복사 시 현재 테마를 반영하도록 수정했습니다.
- 다크 모드 화면에서 적용되는 SVG 필터(`invert(0.88) hue-rotate(180deg)`)를 클립보드 PNG 생성에도 동일하게 적용합니다.
- 일반 구조 셀, 공통 구조 미리보기 모달, 특허 상세 미리보기의 이미지 복사 경로를 함께 반영했습니다.

## 추가 변경 파일

- `frontend/src/components/common/CompoundStructureView.tsx`
- `frontend/src/components/common/StructurePreviewModal.tsx`
- `frontend/src/pages/PatentAnalysisDetail.tsx`

## 다크 모드 Primary 버튼 텍스트 색상

- 다크 모드에서 orange primary 버튼의 텍스트와 아이콘이 검정색으로 보이는 문제를 수정했습니다.
- `token.colorBgContainer`를 inline color로 쓰는 버튼도 덮을 수 있도록 다크 모드 `.ant-btn-primary` 텍스트/아이콘 색상을 흰색으로 강제했습니다.

## 추가 변경 파일

- `frontend/src/index.css`
