# 2026-06-01 작업 보고서

## ChemDraw editor footer controls

- `frontend/src/components/common/ChemDrawModal.tsx`
  - ChemDraw editor 팝업 상단에 있던 좌우/상하 반전 버튼을 하단 footer 영역으로 이동했습니다.
  - footer를 좌우 flex 레이아웃으로 변경해 반전 버튼은 좌측 정렬, 취소/확인(검색) 버튼은 우측 정렬되도록 구성했습니다.
  - 기존 반전 기능, tooltip, editor 준비 전 비활성화 동작은 유지했습니다.

## Reaction Predictor UI planning

- `docs/prototype/reaction_predictor.png` 스케치를 확인해 신규 `Reaction Predictor / OA` 화면의 UI/UX 계획을 작성했습니다.
- 계획 문서: `docs/reaction_predictor_ui_plan.md`
- 기존 프로젝트 공통 틀(`MainLayout`, breadcrumb, `v-table-card`, `v-search-input`, `v-action-btn`, Ant Design Table/Modal)을 기준으로 메뉴, 라우트, 페이지 구조, modal, 결과 패널, CSS 적용 원칙을 정리했습니다.

## Reaction Predictor UI implementation

- `frontend/src/pages/ReactionPredictor.tsx`
  - 신규 `Reaction Predictor` 화면을 추가했습니다.
  - 검색, 반응 타입 전환, 최근 계산 목록, 선택 결과 요약 패널, prediction 추가 modal을 구현했습니다.
  - modal에는 기존 `ChemDrawEditor`를 연결하고, 감지된 C-X site 선택 및 mock prediction 생성 흐름을 구성했습니다.
- `frontend/src/mocks/reactionPredictions.ts`
  - OA/SNAr 예측 목록, site, confidence report, factor contribution mock 데이터를 추가했습니다.
- `frontend/src/App.tsx`
  - `/reaction-predictor` 라우트를 추가했습니다.
- `frontend/src/components/layout/MainLayout.tsx`
  - `Compounds` 하위 메뉴에 `Reaction Predictor` 항목을 추가했습니다.
- `frontend/src/index.css`
  - Reaction Predictor 전용 레이아웃, 결과 카드, molecule preview, confidence report, modal 스타일을 추가했습니다.

## Reaction Predictor prediction modal width

- `frontend/src/pages/ReactionPredictor.tsx`
  - Prediction modal width를 `min(1180px, calc(100vw - 48px))`로 넓혀 ChemDraw editor toolbar와 canvas가 잘리지 않도록 조정했습니다.
  - modal 내부 좌측 Structure Input 영역 비율을 `lg=15`, 우측 site 영역을 `lg=9`로 변경했습니다.
  - ChemDraw editor 높이를 `320px`로 늘렸습니다.
- `frontend/src/index.css`
  - Prediction modal column에 `min-width: 0`을 적용해 좁은 화면에서도 내부 영역이 불필요하게 밀리지 않도록 했습니다.

## Reaction Predictor detected site row toggle

- `frontend/src/pages/ReactionPredictor.tsx`
  - Prediction modal의 Detected C-X Sites 기본 `Use` checkbox 상태를 모두 unchecked로 변경했습니다.
  - site table row 클릭 시 해당 row의 `Use` checkbox가 checked/unchecked 토글되도록 변경했습니다.
  - checkbox 직접 클릭 시 row click과 중복 토글되지 않도록 event propagation을 차단했습니다.
- `frontend/src/index.css`
  - 선택된 detected site row의 배경색을 추가했습니다.

## Reaction Predictor structure search modal

- `frontend/src/pages/ReactionPredictor.tsx`
  - 상단 `구조 검색` 버튼이 `MyBoard`와 동일하게 공통 `ChemDrawModal`을 호출하도록 변경했습니다.
  - ChemDraw 구조 검색 confirm 시 추출된 SMILES를 검색어로 반영하고 modal을 닫도록 연결했습니다.
  - `Add prediction` 버튼은 기존 Prediction 팝업 호출을 유지해 구조 검색과 prediction 생성 흐름을 분리했습니다.

## Reaction Predictor table column layout

- `frontend/src/pages/ReactionPredictor.tsx`
  - Recent calculations 테이블 컬럼을 기본 중앙 정렬로 맞췄습니다.
  - `ΔΔG‡ (kJ/mol)` 컬럼만 우측 정렬로 변경했습니다.
  - `Major site` 컬럼에서 leaving group 텍스트를 제거하고 site tag만 표시하도록 변경했습니다.
  - `Molecule` 컬럼 폭을 넓혔습니다.
- `frontend/src/index.css`
  - Molecule compact SVG preview 크기를 약 3배로 키웠습니다.
  - Molecule 컬럼 셀의 좌우 padding을 최소화했습니다.

## Reaction Predictor molecule common component

- `frontend/src/pages/ReactionPredictor.tsx`
  - `Molecule` 컬럼을 `MyBoard`의 화합물 구조 컬럼과 동일한 공통 `CompoundStructureView` 컴포넌트로 변경했습니다.
  - preview/copy action button은 표시하지 않도록 `showPreviewAction={false}`, `showCopyAction={false}`를 적용했습니다.
- `frontend/src/index.css`
  - 기존 Reaction Predictor 전용 compact molecule preview 스타일을 제거했습니다.

## Reaction Predictor factor card text alignment

- `frontend/src/index.css`
  - `Why ... wins` 영역의 factor row 내부 텍스트를 중앙 정렬했습니다.
  - 긴 설명 텍스트가 잘리지 않도록 ellipsis/nowrap을 제거하고 줄바꿈이 가능하도록 변경했습니다.
  - factor row의 label/detail/percent 컬럼 폭을 조정했습니다.

## Prediction modal ChemDraw flip controls

- `frontend/src/components/common/ChemDrawEditor.tsx`
  - flip 버튼 배치를 `top` 또는 `left`로 선택할 수 있는 `flipControlsPlacement` prop을 추가했습니다.
  - 기본값은 기존과 동일한 상단 가로 배치로 유지했습니다.
- `frontend/src/pages/ReactionPredictor.tsx`
  - Prediction modal 안의 ChemDraw editor만 `flipControlsPlacement="left"`를 적용해 좌우/상하 반전 버튼을 ChemDraw 영역 좌측에 세로 정렬했습니다.

## Prediction modal ChemDraw width

- `frontend/src/pages/ReactionPredictor.tsx`
  - ChemDraw 도구창이 잘리지 않도록 Prediction modal width를 `min(1360px, calc(100vw - 48px))`로 확장했습니다.
  - Structure Input 컬럼을 `lg=16`, Detected C-X Sites 컬럼을 `lg=8`로 조정해 ChemDraw 영역 가로폭을 더 확보했습니다.

## Reaction Predictor search toolbar toggle placement

- `frontend/src/pages/ReactionPredictor.tsx`
  - `Oxidative Addition (OA) / SNAr` 토글을 특허 분석 페이지와 동일하게 검색어 입력 좌측으로 이동했습니다.
  - 토글을 별도 우측 컬럼에서 검색 toolbar flex row 안으로 옮겼습니다.
- `frontend/src/index.css`
  - `reaction-type-tabs` 폭을 content 기준으로 조정해 `SNAr` 우측에 생기던 공백을 제거했습니다.

## Reaction Predictor menu placement

- `frontend/src/components/layout/MainLayout.tsx`
  - `Reaction Predictor` 메뉴를 `Compounds` 하위 메뉴에서 제거했습니다.
  - 좌측 사이드바의 `통합검색` 바로 아래 독립 메뉴로 이동했습니다.
  - full sidebar와 mini sidebar 메뉴 구성을 모두 동일하게 반영했습니다.

## Reaction Predictor page navigation

- `frontend/src/pages/ReactionPredictor.tsx`
  - 화면 상단 breadcrumb/navigation을 `Compounds / Reaction Predictor`에서 `Reaction Predictor` 단독 항목으로 변경했습니다.

## Prediction modal height

- `frontend/src/pages/ReactionPredictor.tsx`
  - Prediction modal 내부 ChemDraw editor 높이를 `320px`에서 `460px`로 늘렸습니다.
- `frontend/src/index.css`
  - Prediction modal body 영역에 viewport 기준 max-height와 세로 스크롤을 적용했습니다.

## ChemDraw structure search flip controls

- `frontend/src/components/common/ChemDrawModal.tsx`
  - 구조 검색 공통 ChemDraw 팝업의 좌우/상하 반전 버튼을 footer 좌측에서 ChemDraw 영역 좌측으로 이동했습니다.
  - Prediction 팝업과 동일하게 버튼을 세로 정렬했습니다.
  - footer는 취소/확인 버튼만 우측 정렬로 표시되도록 정리했습니다.

## SAR Table / My Board structure UX planning

- `docs/sar_myboard_structure_ux_plan.md`
  - SAR Table 화합물 영역의 이미지 크기, 회전, 겹침 컨트롤 변경 계획을 작성했습니다.
  - 그룹별 설정 저장을 `useBoardStore`에 추가하는 방향으로 정리했습니다.
  - MyBoard 화합물 구조 컬럼이 SAR Table에서 저장한 회전/크기 값을 반영하도록 하는 계획을 작성했습니다.
  - MyBoard 구조 컬럼의 hover action, border/background 제거, padding 최소화 계획을 정리했습니다.

## Structure UX planning update

- `docs/sar_myboard_structure_ux_plan.md`
  - 회전 컨트롤 step을 `30°`로 확정했습니다.
  - `SynthesisBoard`의 table/card 구조 렌더링도 SAR Table에서 저장한 그룹별 회전 설정을 동일하게 반영하는 범위로 추가했습니다.

## SAR/MyBoard/Synthesis structure UX implementation

- `frontend/src/store/useBoardStore.ts`
  - 그룹별 구조 표시 설정(`groupStructureViewSettings`)과 업데이트 action을 추가했습니다.
  - SAR 이미지 크기, 회전, 겹침, MyBoard 이미지 크기 값을 그룹별로 저장하도록 했습니다.
- `frontend/src/components/common/CompoundStructureView.tsx`
  - 구조 SVG/ChemDraw preview에 `structureStyle`을 적용할 수 있게 했습니다.
  - action button을 기존 rail 또는 hover overlay로 표시할 수 있는 `actionPlacement` 옵션을 추가했습니다.
- `frontend/src/pages/SarTable.tsx`
  - 화합물 이미지 크기 설정을 `- | input | +` 구성으로 변경했습니다.
  - 이미지 크기 범위를 `60%~130%`, step `5%`로 적용했습니다.
  - 회전 컨트롤을 추가하고 `30°` step으로 적용했습니다.
  - 겹침 컨트롤을 추가하고 `0~30%`, step `5%`로 적용했습니다.
  - 2줄 모드에서는 겹침 컨트롤을 비활성화했습니다.
  - 단일 그룹 선택 시에만 설정 변경이 가능하고, 여러 그룹 선택 시 빈값/disabled가 되도록 했습니다.
  - 화합물 카드 더블클릭 확대 팝업 호출을 제거했습니다.
- `frontend/src/pages/MyBoard.tsx`
  - SAR Table에서 저장한 그룹별 회전 값을 화합물 구조 컬럼에 반영했습니다.
  - 그룹 상세 목록 toolbar에 구조 이미지 크기 컨트롤을 추가했습니다.
  - MyBoard 구조 이미지 크기 범위를 `70%~120%`, step `5%`로 적용했습니다.
  - 화합물 구조 컬럼의 action button을 hover overlay로 변경했습니다.
  - 구조 frame border/background와 셀 padding을 줄였습니다.
- `frontend/src/pages/SynthesisBoard.tsx`
  - table mode와 draw/card mode의 구조 표시에도 그룹별 회전 값을 반영했습니다.
- `frontend/src/index.css`
  - 공통 구조 hover overlay action 스타일을 추가했습니다.

## MyBoard group list structure column update

- `frontend/src/pages/MyBoard.tsx`
  - 그룹 리스트의 `화합물 구조` 컬럼도 그룹 상세 목록과 동일하게 그룹별 구조 크기 설정과 SAR Table에서 저장한 회전 값을 반영하도록 변경했습니다.
  - 대표 구조 컬럼의 frame border/background를 제거하고 action button을 hover overlay 방식으로 표시하도록 맞췄습니다.
  - 그룹 리스트 구조 컬럼의 좌우/상하 padding을 `4px` 기준으로 줄였습니다.

## SAR overlap and SynthesisBoard structure column update

- `frontend/src/pages/SarTable.tsx`
  - 화합물 겹침 설정이 켜져 있을 때 카드, 구조 영역, 구조 frame 배경을 투명하게 처리해 뒤쪽 SVG가 가려지지 않도록 변경했습니다.
  - hover/selected 상태에서도 겹침 모드에서는 배경색을 다시 덮지 않도록 CSS를 조정했습니다.
- `frontend/src/pages/SynthesisBoard.tsx`
  - 합성보드 테이블의 `화합물 구조` 컬럼도 MyBoard 상세 목록과 동일하게 action button을 hover overlay 방식으로 변경했습니다.
  - 구조 frame border/background를 제거하고 셀 padding을 `4px` 기준으로 줄였습니다.

## SAR compound structure controls UI update

- `frontend/src/pages/SarTable.tsx`
  - 화합물 구조 영역의 크기/회전/겹침 컨트롤을 샘플 이미지(`sample/ui/img.png`) 기준의 3줄 스택 UI로 변경했습니다.
  - 각 행은 `아이콘 버튼 | 값 표시 | 아이콘 버튼` 구성을 사용하도록 정리했습니다.
  - 기존 크기 `5%` step, 회전 `30°` step, 겹침 `5%` step 및 2줄 모드 겹침 비활성화 동작은 유지했습니다.
  - 컨트롤 그룹 배치를 세로 스택에서 가로 배치로 변경했습니다.

## MyBoard structure scale controls UI update

- `frontend/src/pages/MyBoard.tsx`
  - 그룹 상세 목록 toolbar의 구조 크기 조절 영역을 SAR Table과 동일한 `버튼 | 값 표시 | 버튼` 형태로 변경했습니다.
  - 기존 `70%~120%`, `5%` step 동작과 단일 그룹 선택 시에만 활성화되는 조건은 유지했습니다.
  - 구조 크기 조절 기본 표시값을 `120%`에서 `100%`로 변경했습니다.

## MyBoard group list structure image fixed size

- `frontend/src/pages/MyBoard.tsx`
  - 그룹 리스트의 `화합물 구조` 컬럼 대표 구조 이미지 크기를 `130 x 97.5`로 고정했습니다.
  - 그룹 상세 목록의 구조 크기 조절 설정은 기존대로 유지했습니다.

## MyBoard collapsed group structure panel sizing

- `frontend/src/pages/MyBoard.tsx`
  - 그룹 리스트를 `화합물 구조만 보기`로 접었을 때 패널 폭을 고정 이미지 크기에 맞게 확장했습니다.
  - 접힘 상태 전용 구조 컬럼 폭을 조정하고, 구조 frame이 `100%`로 늘어나는 스타일을 `130 x 97.5` 고정 크기로 변경했습니다.

## MyBoard detail structure rotated row height

- `frontend/src/pages/MyBoard.tsx`
  - 그룹 상세 목록의 `화합물 구조` 컬럼에서 회전 각도에 따라 필요한 bounding box 높이를 계산하도록 했습니다.
  - 회전 후 구조 높이가 증가하면 row 높이도 함께 늘어나도록 wrapper의 `minHeight`를 조정했습니다.
  - 구조 자체 비율이 변하지 않도록 frame 크기는 유지하고 overflow만 열어 회전된 구조가 잘리지 않도록 했습니다.

## SAR compound card group rotation

- `frontend/src/pages/SarTable.tsx`
  - 화합물 카드 렌더링 시 공통 active 그룹 각도 대신 각 compound의 `groupId`에 저장된 `sarRotationDeg`를 적용하도록 변경했습니다.
  - 여러 그룹을 동시에 볼 때 그룹별 회전 각도가 서로 다르면 각 그룹 설정에 맞춰 화합물 구조가 표시됩니다.

## Structure scale baseline adjustment

- `frontend/src/pages/SarTable.tsx`
  - 화합물 영역 크기 조절에서 표시값 `100%`가 기존 `95%` 실제 크기와 같도록 렌더링 배율 기준을 조정했습니다.
- `frontend/src/pages/MyBoard.tsx`
  - 그룹 상세 목록 구조 크기 조절에서도 표시값 `100%`가 기존 `95%` 실제 크기 기준이 되도록 동일한 배율 보정을 적용했습니다.
  - 그룹 상세 목록 `화합물 구조` SVG 표시 크기를 기존 대비 `1.05배` 키웠습니다.

## MyBoard group list rotated structure visibility

- `frontend/src/pages/MyBoard.tsx`
  - 그룹 리스트의 `화합물 구조` 컬럼 대표 구조는 `130 x 97.5` 고정 frame을 유지하도록 되돌렸습니다.
  - SVG 회전 시 고정 frame 안에서 잘리지 않도록 frame overflow를 열고, 회전 bounding box 기준으로 wrapper의 최소 너비/높이를 확보하도록 수정했습니다.

## Scrollbar color consistency

- `frontend/src/pages/SarTable.tsx`
  - 화합물 영역 가로 스크롤에 테마 토큰 기반 scrollbar 색상을 적용해 light/dark 모드에서 다른 UI와 맞도록 조정했습니다.
- `frontend/src/pages/MyBoard.tsx`
  - 테이블 컬럼 설정 팝업 body scrollbar도 동일한 토큰 기반 색상으로 맞췄습니다.

## MyBoard design modal ChemDraw controls

- `frontend/src/pages/MyBoard.tsx`
  - 그룹 상세 목록 `Add` 버튼으로 여는 디자인 등록 팝업의 ChemDraw editor에 `flipControlsPlacement="left"`를 적용했습니다.
  - 좌/우, 상/하 반전 버튼이 ChemDraw 공통 팝업처럼 editor 좌측에 세로 배치됩니다.

## MyBoard structure hover action placement

- `frontend/src/pages/MyBoard.tsx`
  - My Board의 화합물 구조 컬럼들에서 이미지 hover 시 표시되는 action button overlay 위치를 우측 상단에서 우측 하단으로 변경했습니다.

## SAR group structure panel UX alignment

- `frontend/src/pages/SarTable.tsx`
  - 좌측 그룹 리스트의 `화합물 구조` 영역을 My Board 그룹 리스트 접힘 상태 UX와 동일한 기준으로 조정했습니다.
  - 대표 구조 frame을 `117 x 87.75`로 맞추고, 회전 시 bounding box가 최대 표시 영역을 넘으면 SVG를 축소해 표시하도록 했습니다.
  - 그룹별 저장된 회전 각도를 좌측 그룹 리스트 대표 구조에도 반영했습니다.

## SynthesisBoard detail structure hover actions

- `frontend/src/pages/SynthesisBoard.tsx`
  - 합성보드 상세 목록 테이블의 기존 `화합물 구조` 컬럼에서 mouse over 시 표시되는 action button overlay 위치를 우측 하단으로 변경했습니다.

## MyBoard group list rotated structure scale down

- `frontend/src/pages/MyBoard.tsx`
  - 그룹 리스트 `화합물 구조` 컬럼에서 회전 bounding box가 지정된 최대 표시 영역을 넘으면 SVG를 축소해서 표시하도록 변경했습니다.
  - 회전으로 row 높이가 과도하게 늘어나지 않도록 최대 표시 영역 기준으로 wrapper 크기와 구조 scale을 함께 계산합니다.
  - 그룹 리스트 화합물 구조 기본 frame을 `117 x 87.75`로 조정했습니다.
  - frame 축소에 맞춰 그룹 리스트 구조 컬럼 폭과 접힘 상태 패널 폭을 줄여 좌우 여백을 줄였습니다.
  - 접힘 상태 구조 전용 컬럼 폭과 패널 폭을 추가로 축소했습니다.
