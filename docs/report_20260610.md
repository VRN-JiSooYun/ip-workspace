## Compound Search API 컨테이너 의존성 오류 수정

- Docker 컨테이너 실행 시 `compound_search/elastic_search_agent.py`가 `analyzer.hier_pattern.AtomCodeWords`를 import하면서 `networkx` 누락으로 실패하는 문제를 확인했습니다.
- `analyzer/hier_pattern.py`는 top-level에서 `networkx`와 `matplotlib`를 import하므로 두 패키지를 `docker/compound-search-api.Dockerfile`의 컨테이너 전용 설치 단계에 추가했습니다.
  - `networkx==3.6.1`
  - `matplotlib==3.10.8`
- `compound_search/requirement.txt`는 수정하지 않았습니다.

## 특허검색 Reference 팝업 이미지 순서 변경

- 특허검색 결과 리스트에서 reference 팝업을 열었을 때 이미지 영역 표시 순서를 `Genus Markush`, `Scaffold` 순서로 변경했습니다.
- 렌더링 가능한 이미지가 없는 항목은 기존처럼 표시하지 않습니다.

## 특허검색 Reference 팝업 특허번호 hover 스타일

- Reference 팝업의 클릭 가능한 특허 번호에 mouse hover 시 underline이 표시되도록 스타일을 추가했습니다.

## 특허검색 Reference 팝업 Source Type 분기

- compound search `/search` 결과 row에 포함된 `sources/source_list` 데이터를 reference 팝업에 바인딩했습니다.
- 별도 하단 source 섹션 대신 `source_type` 값을 섹션 title로 사용해 Patent API item과 row source item을 source type별로 분기 표시하도록 수정했습니다.
- 좌측 `Source Type` 필터에 Patent API 결과의 source type과 row source의 `source_type`을 함께 표시하고 필터링할 수 있게 했습니다.
- 각 source row에 `source_name`, `source_link`를 표시하고, URL 형태의 `source_link`는 새 탭 링크로 열 수 있게 했습니다. URL이 아닌 `source_link` 값도 텍스트로 표시합니다.
- source row에 `vora_link` 값이 함께 내려오는 경우 `vora_link`도 화면에 표시하고, URL이면 새 탭 링크로 열 수 있게 했습니다.
- row source의 `source_type`이 `patent`인 경우는 row source UX에서 제외하고, `/patents` API 조회 결과만 표시하도록 했습니다.
- source type 원본값이 `substance`인 경우 화면에서는 `Patent`로 표시하도록 정규화했습니다.
- Patent API 결과는 `patent_type` 값이 있더라도 source type 섹션 title을 `Patent`로 고정했습니다.
- source type 섹션 title과 좌측 filter 라벨은 첫 글자가 대문자로 보이도록 정규화했습니다.
- 좌측 `Source Type` 필터가 전체 선택 상태이면 버튼이 `전체 해제`로 바뀌고 클릭 시 모든 선택을 해제하도록 수정했습니다.
- 검색 결과 card/full row 상단의 `...` source link 버튼은 제거했습니다.

## UniversalSearch TypeScript 빌드 오류 수정

- `UniversalSearch.tsx`에서 `CompoundSearchSource` 타입을 사용하면서 import하지 않아 Docker build의 `tsc -b`가 실패하던 문제를 수정했습니다.
- `source_link/vora_link` 배열 필터의 type predicate가 문자열을 반환할 수 있던 조건식을 `value.trim().length > 0` boolean 조건으로 변경했습니다.
- 통합 검색 결과 카드/리스트의 화합물 구조 SVG 표시 높이와 RDKit 최소 렌더링 크기를 약 1.4배 확대했습니다.
- 통합 검색 검색 방식 옵션에 `Similarity`를 추가하고 API 요청 타입 `similarity`를 허용하도록 수정했습니다.
- 통합 검색 입력 형식이 `Molblock`이어도 SMILES와 동일한 단일 `Input` 컴포넌트를 사용하도록 정리했습니다.
- 통합 검색 입력 형식이 `Molblock`일 때 검색 input을 비활성화하고, ChemDraw 구조 검색 결과의 SMILES 표시값은 유지하도록 조정했습니다.
- 통합 검색 입력 형식 기본값과 초기화 값을 `Molblock`으로 변경했습니다.
- 통합 검색 구조 검색 모달에서 ChemDraw 구조를 확정하면 검색 input에는 SMILES를 넣고, 내부 상태에는 SMILES/Molblock을 모두 보관해 검색 요청 시 현재 입력 형식에 맞는 값을 전달하도록 추가했습니다.
- ChemDraw 구조 미리보기 SVG root를 100% 크기로 정규화하고 `compound-structure-svg` 영역을 꽉 채우도록 조정했습니다.
- 통합 검색 결과 구조 이미지 확대 preview 모달과 SVG 표시 영역을 더 크게 조정했습니다.

## Patent Analysis Summary API 데이터 연결

- 특허 분석 상세 Summary 탭의 `Patent Analysis Summary`가 `frequency_analysis_result_json`을 런타임 변환해 사용하도록 연결했습니다.
- `parent_scaffold`, `rank1/rank2/...`, `r_groups`를 기존 Summary UI shape로 변환하는 helper를 추가했습니다.
- API 값이 없거나 파싱할 수 없으면 기존 mock 데이터를 사용하지 않고 `Patent Analysis Summary 데이터가 없습니다.` empty UX를 표시합니다.
- 페이지 로드 시 상세 화면이 mock dataset/PDF/highlight를 fallback으로 표시하지 않도록 제거했습니다.

## Patent Analysis Pagination 기본값 변경

- 특허 분석 리스트 페이지의 기본 row 조회 수를 10개에서 30개로 변경했습니다.
- 특허 분석 상세 페이지 Raw Data 탭의 테이블/카드 기본 page size를 30개로 변경했습니다.

## Patent Analysis List 컬럼 추가

- 특허 분석 리스트에서 별표 아이콘 컬럼을 제거했습니다.
- 특허 분석 리스트에 `실시예 수`, `Key Scaffold`, `AI Key Compound`, `Analysis Date` 컬럼을 추가했습니다.
- 리스트 API row의 `num_embodiment`, `key_scaffold_img`, `ai_key_compound_*`, `analysis_date` 계열 필드를 화면 표시용으로 매핑했습니다.
- `Analysis Date` 컬럼에 API `date_created` 값을 우선 매핑하도록 추가했습니다.
- `Key Scaffold`, `AI Key Compound` SVG 컬럼을 My Board 화합물 구조 컬럼과 동일한 frameless/overlay 액션 스타일로 조정했습니다.
- `Key Scaffold`, `AI Key Compound` SVG 컬럼의 width/height를 축소해 리스트 밀도를 높였습니다.
- `Key Scaffold`, `AI Key Compound` 이미지 영역이 컬럼 내부 폭을 채우도록 조정했습니다.
- `Key Scaffold`, `AI Key Compound` SVG 렌더링 시 root `width/height`와 직접 하위 `rect` 영역을 viewBox 기준으로 정규화하고, 이미지 프레임을 정사각형(`168x168`)으로 표시하도록 조정했습니다.
- 특허 분석 리스트 일반 목록에서 `타겟` 컬럼을 `제목` 앞으로 이동하고 `작업` 컬럼을 제거했습니다.

## Patent Analysis Detail Empty UX 추가

- 특허 분석 상세 Summary 탭의 `추천 Key Compound (빈도수/중요도 기반)` 데이터가 없을 때 빈 스크롤 영역 대신 `추천 Key Compound 데이터가 없습니다.` Empty UX를 표시하도록 변경했습니다.
- 특허 분석 상세 페이지 split layout 비율을 localStorage에 저장/복원하지 않고, 페이지 진입 시 viewport preset의 기본값으로 열리도록 변경했습니다.
- 특허 분석 상세 페이지의 SVG 구조 영역을 My Board 화합물 구조 영역처럼 frameless/overlay 스타일로 맞추고, 좌우/상하 여백을 줄이기 위해 SVG 표시 크기를 확대했습니다.
- 공통 `CompoundStructureView`의 overlay 액션을 My Board와 같은 하단 hover 버튼 UX로 기본화하고, 특허 분석 상세 페이지의 Summary/Raw Data SVG 영역을 동일 컴포넌트로 통일했습니다.
- Summary 탭의 Scaffold Ranking 및 Scaffold Rank 1 SVG는 Raw Data 확대 스타일에서 제외해 이미지가 프레임 안에서 잘리지 않도록 조정했습니다.
- Scaffold Ranking의 Parent Scaffold/Rank 카드 높이와 Scaffold Rank 1 프레임 크기를 키우고, Summary SVG 최대 표시 크기를 92%로 제한해 경계 잘림을 추가로 방지했습니다.
- Summary 탭과 Scaffold 카드에 전용 클래스를 추가하고 `ant-card-body`/구조 프레임의 overflow 및 높이 제한을 해제해 SVG 영역 확대 시 잘림을 방지했습니다.
- Scaffold Ranking 영역은 카드가 줄어들 때 SVG가 카드 밖으로 뚫고 나오지 않도록 고정 폭 카드 리스트와 가로 스크롤 UX로 변경했습니다.
- Raw Data의 R-group/Structure/Scaffold SVG 프레임을 정사각형으로 통일하고, 프레임 내부 SVG를 중앙 `contain` 표시로 조정해 화합물 구조가 잘리지 않도록 수정했습니다.
- Raw Data/Clean Data 테이블에서 row 선택/hover 후 가로 스크롤 시 fixed 컬럼과 스크롤 영역이 겹쳐 보이지 않도록 fixed cell z-index와 불투명 배경 레이어를 보강했습니다.
- 추천 Key Compound 카드에 정사각형 이미지 모드를 적용하고 카드 폭/구조 영역을 키워 SVG가 더 크게 보이도록 조정했습니다.
