# MyBoard 그룹/공유 컬럼 폭 보정

## 변경 내용
- `frontend/src/pages/MyBoard.tsx`
  - 그룹 리스트 테이블의 `그룹` 컬럼 폭을 38px에서 46px로 확대했다.
  - 그룹 리스트 테이블의 `공유` 컬럼 폭을 38px에서 46px로 확대했다.

## 목적
- 일부 모니터 해상도/브라우저 스케일에서 `그룹`, `공유` 헤더가 2줄로 줄바꿈되는 현상을 줄인다.

## 확인 필요
- 로컬 실행은 사용자가 진행한다.
- 문제 해상도에서 두 헤더가 한 줄로 표시되는지 확인 필요.
