# Report 2026.06.05

## MyBoard data button alignment

- Updated the MyBoard detail table `데이터` column so quick viewer buttons are grouped by side.
- `KP` is rendered in the left group, while `PDB`, `Docking`, and `MD` are rendered in the right group.
- When only one side has buttons, the visible button group is centered in the data cell.
- Expanded the data column width to keep the full button set on one line.
- Updated the right data button group to stack vertically and tightened the spacing between left and right groups.
- Prevented data buttons, especially `KP`, from shrinking or wrapping when paired with right-side buttons.
- Matched the `KP` button color treatment with the right-side data buttons.

## MyBoard design compound filter mock data

- Updated MyBoard detail filtering so `Design` shows rows with an empty `물질 번호 (VRN)` value and `Compound` shows only rows with a `물질 번호 (VRN)` value.
- Changed CSV-based mock compound generation so only every sixth mock row keeps a VRN value; the rest render the VRN column as blank.
- Kept a fallback design name for blank-VRN mock rows so row labels, search, and quick viewer titles remain usable.

## MyBoard quick viewer split pane

- Changed Quick Viewer from a fixed right-side overlay into an in-layout right pane so it takes space instead of covering the main MyBoard area.
- Added a vertical resize handle between the main MyBoard workspace and Quick Viewer pane.
- Persisted the Quick Viewer pane width in local storage and added keyboard resizing support on the split handle.
- On responsive widths, Quick Viewer opens as a full-screen pane.
- Restored MyBoard group/detail table horizontal scrolling in responsive layouts by forcing table content overflow-x inside the Ant Design table containers.
- Applied theme-aware light/dark scrollbar styling to the responsive MyBoard page scroll, table scroll containers, and Quick Viewer body.

## ChemDraw flip tooltip placement

- Moved the left/right and up/down flip button tooltips to the left side of the buttons in the common ChemDraw modal.
- Applied the same tooltip placement to the reusable inline ChemDraw editor.

## MyBoard Quick Viewer Mol* planning

- Added `docs/myboard_quick_viewer_molstar_plan.md`.
- Planned Mol* canvas-only integration for the Quick Viewer `PDB` tab using the sample `sample/quick_viewer/6LUB.cif` file.
- Covered PDB/CIF format handling, mock data changes, component structure, responsive behavior, risks, and verification checklist.

## MyBoard Quick Viewer Mol* implementation

- Added `MolstarStructureViewer` for canvas-only Mol* structure rendering.
- Wired the Quick Viewer `PDB` tab to render Mol* when a structure URL is present and fall back to the existing mock SVG otherwise.
- Added PDB/CIF structure payload fields to quick viewer mock assets.
- Copied the sample `6LUB.cif` into `frontend/public/quick_viewer/6LUB.cif` for Vite runtime serving.
- Added Mol* canvas stage styles for split-pane and responsive fullscreen Quick Viewer layouts.

## SAR table viewport scroll sizing

- Updated the SAR Table bottom data table to calculate its body height from the current viewport.
- The table now subtracts the table card toolbar, multi-level header, and bottom gap from the available screen height.
- Always passes a vertical `scroll.y` value to Ant Design Table so the horizontal scrollbar stays inside the visible table area without requiring full-page vertical scrolling.
- In responsive width, the table body now keeps at least 10 visible rows plus scrollbar space.

## MyBoard Mol* warning cleanup

- Removed the Ant Design `Spin` `tip` usage that triggered the browser console warning in the Mol* loading overlay.
- Added a Mol* post-load cleanup step that removes generated polymer gap representations from the state tree before resetting the camera.
- Changed the PDB viewer load path to start from an empty representation preset and add only a cartoon `polymer-trace` visual, preventing `polymer-gap` missing-segment connectors from being created.
- Added a separate ligand component with a `ball-and-stick` representation so ligands remain visible while polymer missing-gap connectors stay hidden.
- Set the polymer cartoon trace default color to uniform `#00FF00`.
- Suppressed the Mol* internal `rawReturn()` performance warning emitted during structure click focus/clear interactions.
- Added a custom Mol* hover tooltip overlay that shows residue-level labels from the hovered loci in the canvas-only Quick Viewer.
- Guarded the hover tooltip residue granularity call so Mol* builds without `Loci.Granularity.residue` fall back to the original hovered loci instead of throwing.
- Fixed the Mol* hover tooltip TypeScript build error by accessing optional `Loci.Granularity.residue` through a narrowed runtime shape instead of the Mol* static type.
- Added targeted suppression patterns for Mol* WebGL readback warnings and ChemDraw non-passive wheel listener warnings emitted by third-party internals.

## Quick Viewer VORA link

- Updated the Quick Viewer `PDB` CTA so `VORA 가기` opens the same VORA URL as the sidebar button.
- Added the active PDB ID as a `pdb` query parameter, for example `https://voronoi.app/vora/?pdb=6lub`.
- Increased the desktop Quick Viewer resize maximum width from 620px to 868px, about 1.4x wider.

## SAR RDKit cluster highlight

- Added a frontend RDKit `/cluster` service with request/result caching and in-flight request de-duplication.
- Connected SAR compound card `Com` mode to `/cluster` with common scaffold highlighting.
- Connected SAR compound card `Diff` mode to `/cluster` with reverse highlighting for differing regions.
- Added a rendered SVG override path to `CompoundStructureView` so clustered highlight SVGs can render without polluting normal RDKit `/draw` caches.
- Added stale-response protection and compact loading/error feedback near the SAR compound highlight toggle.
- Confirmed `/cluster` receives the current SAR rotation angle, and replaced the Com/Diff loading fallback with an in-card spinner so Off-state rotated drawings do not appear while cluster SVGs are loading.
- Added MyBoard-style quick viewer data buttons to SAR compound cards and wired them to open the shared Quick Viewer panel from the SAR page.
- Changed the SAR Quick Viewer from a fixed overlay into a MyBoard-style in-layout split pane with draggable width resizing and responsive fullscreen behavior.
- Moved SAR card quick viewer buttons into a horizontal overlay at the bottom of the structure area so the original bottom compound-name text height is preserved.
- Aligned SAR board content cards with the search/filter card by removing the extra right padding inside `sar-board-content`.
- Removed the extra `sar-table-card` bottom margin that duplicated the SAR page bottom padding and caused unnecessary full-page vertical scrolling.
- Expanded the SAR bottom table body to fill the remaining viewport height even when there are only a few rows, while clamping the height to the visible screen area to avoid introducing full-page scrolling.
- Refined the SAR table height calculation to use the actual `.ant-table-body` top and the SAR page content bottom, excluding page bottom padding so the expanded table does not create a page-level y scrollbar.
- Updated the SAR table height policy so the table fills the remaining viewport when space is available, but keeps at least 10 visible rows plus horizontal-scrollbar space when upper card sections grow, allowing page-level y scrolling in that constrained case.
